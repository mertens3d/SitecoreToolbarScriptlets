export interface IGuidHelper {
    ParseGuid(AsString: string): import("./IGuid").IGuid;
    ParseGuid(AsString: string): import("./IGuid").IGuid;
    ParseGuid(AsString: string): import("./IGuid").IGuid;
    EmptyGuid();

    NewGuid(): import("./IGuid").IGuid;

    NewGuid(): import("./IGuid").IGuid;
}
