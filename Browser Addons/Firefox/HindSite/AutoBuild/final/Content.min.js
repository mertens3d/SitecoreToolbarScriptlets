/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, { enumerable: true, get: getter });
/******/ 		}
/******/ 	};
/******/
/******/ 	// define __esModule on exports
/******/ 	__webpack_require__.r = function(exports) {
/******/ 		if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 			Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 		}
/******/ 		Object.defineProperty(exports, '__esModule', { value: true });
/******/ 	};
/******/
/******/ 	// create a fake namespace object
/******/ 	// mode & 1: value is a module id, require it
/******/ 	// mode & 2: merge all properties of value into the ns
/******/ 	// mode & 4: return value when already ns object
/******/ 	// mode & 8|1: behave like require
/******/ 	__webpack_require__.t = function(value, mode) {
/******/ 		if(mode & 1) value = __webpack_require__(value);
/******/ 		if(mode & 8) return value;
/******/ 		if((mode & 4) && typeof value === 'object' && value && value.__esModule) return value;
/******/ 		var ns = Object.create(null);
/******/ 		__webpack_require__.r(ns);
/******/ 		Object.defineProperty(ns, 'default', { enumerable: true, value: value });
/******/ 		if(mode & 2 && typeof value != 'string') for(var key in value) __webpack_require__.d(ns, key, function(key) { return value[key]; }.bind(null, key));
/******/ 		return ns;
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = 0);
/******/ })
/************************************************************************/
/******/ ([
/* 0 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
// ESM COMPAT FLAG
__webpack_require__.r(__webpack_exports__);

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Content/scripts/_first/_ContentManagerBase.js
class ContentManagerBase {
    constructor(contentHub, allAgents) {
        this.ContentHub = contentHub;
        this.AllAgents = allAgents;
    }
    AtticMan() { return this.ContentHub.AtticMan; }
    OneScWinMan() { return this.ContentHub.OneWindowMan; }
    ScUiMan() { return this.ContentHub.SitecoreUiMan; }
    MsgMan() { return this.ContentHub.MsgMan; }
    ContentFactory() { return this.ContentHub.ContentFactory; }
    MiscMan() { return this.ContentHub.MiscMan; }
    PromiseOneStep() { return this.ContentHub.PromiseOneStep; }
    MsgFlag() { return this.ContentHub.MessageFlag; }
    SharedConst() { return this.ContentHub.SharedConst; }
    ReadyForMessages() { return this.ContentHub.ReadyForMessages; }
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Shared/scripts/Enums/scWindowType.js
var scWindowType;
(function (scWindowType) {
    scWindowType[scWindowType["Unknown"] = 0] = "Unknown";
    scWindowType[scWindowType["LoginPage"] = 1] = "LoginPage";
    scWindowType[scWindowType["Desktop"] = 2] = "Desktop";
    scWindowType[scWindowType["ContentEditor"] = 3] = "ContentEditor";
    scWindowType[scWindowType["Launchpad"] = 4] = "Launchpad";
    scWindowType[scWindowType["Edit"] = 5] = "Edit";
    scWindowType[scWindowType["Normal"] = 6] = "Normal";
    scWindowType[scWindowType["Preview"] = 7] = "Preview";
})(scWindowType || (scWindowType = {}));

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Shared/scripts/Enums/SnapShotFlavor.js
var SnapShotFlavor;
(function (SnapShotFlavor) {
    SnapShotFlavor[SnapShotFlavor["Unknown"] = 0] = "Unknown";
    SnapShotFlavor[SnapShotFlavor["Autosave"] = 1] = "Autosave";
    SnapShotFlavor[SnapShotFlavor["Favorite"] = 2] = "Favorite";
    SnapShotFlavor[SnapShotFlavor["Manual"] = 3] = "Manual";
})(SnapShotFlavor || (SnapShotFlavor = {}));

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Shared/scripts/Classes/PromiseResult.js
class PromiseResult {
    constructor(nickname, logger) {
        this.prefix = 'Promise Successful? -> ';
        this.Success = false;
        this.NickName = nickname;
        this.RejectReasonArPvt = [];
        this.Log = logger;
    }
    WasSuccessful() {
        return this.Success;
    }
    MarkSuccessful() {
        this.Success = true;
        if (this.Log) {
            this.Log.LogVal(this.prefix + this.NickName, this.Success);
        }
        else {
            console.log('no logger success');
        }
    }
    MarkFailed(ex) {
        if (ex) {
            ex = ex.toString();
        }
        else {
            ex = '{no fail message}';
        }
        this.RejectReasons += ', ' + ex;
        this.Success = false;
        if (this.Log) {
            this.Log.ErrorAndThrow(this.prefix + this.NickName, this.Success + '  err: ' + ex);
        }
        else {
            console.log('no logger fail ' + ex);
        }
    }
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Shared/scripts/Enums/CacheMode.js
var CacheMode;
(function (CacheMode) {
    CacheMode[CacheMode["DoNotUseCach"] = 0] = "DoNotUseCach";
    CacheMode[CacheMode["OkToUseCache"] = 1] = "OkToUseCache";
})(CacheMode || (CacheMode = {}));

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Shared/scripts/Enums/MessageFlag.js
var MsgFlag;
(function (MsgFlag) {
    MsgFlag[MsgFlag["Unknown"] = 0] = "Unknown";
    MsgFlag[MsgFlag["GetAllStorageOneWindow"] = 1] = "GetAllStorageOneWindow";
    MsgFlag[MsgFlag["NewWindowTest"] = 2] = "NewWindowTest";
    MsgFlag[MsgFlag["Ping"] = 3] = "Ping";
    MsgFlag[MsgFlag["RemoveFromStorage"] = 4] = "RemoveFromStorage";
    MsgFlag[MsgFlag["ReqAddCETab"] = 5] = "ReqAddCETab";
    MsgFlag[MsgFlag["ReqAdminB"] = 6] = "ReqAdminB";
    MsgFlag[MsgFlag["ReqCurState"] = 7] = "ReqCurState";
    MsgFlag[MsgFlag["ReqGoDesktop"] = 8] = "ReqGoDesktop";
    MsgFlag[MsgFlag["ReqLoginWithAdminB"] = 9] = "ReqLoginWithAdminB";
    MsgFlag[MsgFlag["ReqMarkFavorite"] = 10] = "ReqMarkFavorite";
    MsgFlag[MsgFlag["ReqOpenCE"] = 11] = "ReqOpenCE";
    MsgFlag[MsgFlag["ReqQuickPublish"] = 12] = "ReqQuickPublish";
    MsgFlag[MsgFlag["ReqRestoreClick"] = 13] = "ReqRestoreClick";
    MsgFlag[MsgFlag["ReqRestoreToNewTab"] = 14] = "ReqRestoreToNewTab";
    MsgFlag[MsgFlag["ReqSetScMode"] = 15] = "ReqSetScMode";
    MsgFlag[MsgFlag["ReqTakeSnapShot"] = 16] = "ReqTakeSnapShot";
    MsgFlag[MsgFlag["ReqUpdateNickName"] = 17] = "ReqUpdateNickName";
    MsgFlag[MsgFlag["RespCurState"] = 18] = "RespCurState";
    MsgFlag[MsgFlag["RespError"] = 19] = "RespError";
    MsgFlag[MsgFlag["RespListeningAndReady"] = 20] = "RespListeningAndReady";
    MsgFlag[MsgFlag["RespTaskFailed"] = 21] = "RespTaskFailed";
    MsgFlag[MsgFlag["RespTaskSuccessful"] = 22] = "RespTaskSuccessful";
    MsgFlag[MsgFlag["RespTest"] = 23] = "RespTest";
    MsgFlag[MsgFlag["ReqCompactCE"] = 24] = "ReqCompactCE";
})(MsgFlag || (MsgFlag = {}));

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Shared/scripts/Enums/BufferDirection.js
var BufferDirection;
(function (BufferDirection) {
    BufferDirection[BufferDirection["left"] = 0] = "left";
    BufferDirection[BufferDirection["right"] = 1] = "right";
})(BufferDirection || (BufferDirection = {}));

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Shared/scripts/Enums/BufferChar.js
var BufferChar;
(function (BufferChar) {
    BufferChar[BufferChar["space"] = 0] = "space";
    BufferChar[BufferChar["Nbsp"] = 1] = "Nbsp";
    BufferChar[BufferChar["Period"] = 2] = "Period";
    BufferChar[BufferChar["Zero"] = 3] = "Zero";
})(BufferChar || (BufferChar = {}));

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Shared/scripts/Enums/SettingKey.js
var SettingKey_SettingKey;
(function (SettingKey) {
    SettingKey[SettingKey["unknown"] = 0] = "unknown";
    SettingKey[SettingKey["AutoLogin"] = 100] = "AutoLogin";
    SettingKey[SettingKey["__dead101"] = 101] = "__dead101";
    SettingKey[SettingKey["DebugKeepDialogOpen"] = 102] = "DebugKeepDialogOpen";
    SettingKey[SettingKey["AutoSaveIntervalMin"] = 103] = "AutoSaveIntervalMin";
    SettingKey[SettingKey["MaxAutoSaveCount"] = 200] = "MaxAutoSaveCount";
    SettingKey[SettingKey["LogToConsole"] = 201] = "LogToConsole";
    SettingKey[SettingKey["Test"] = 202] = "Test";
    SettingKey[SettingKey["LgndLog"] = 300] = "LgndLog";
    SettingKey[SettingKey["LgndForeSite"] = 301] = "LgndForeSite";
    SettingKey[SettingKey["LgndHindSite"] = 302] = "LgndHindSite";
    SettingKey[SettingKey["LgndSettings"] = 303] = "LgndSettings";
    SettingKey[SettingKey["LgndState"] = 304] = "LgndState";
    SettingKey[SettingKey["LastUsedLogToStorageKey"] = 999] = "LastUsedLogToStorageKey";
    SettingKey[SettingKey["AutoSnapshotBeforeWindowChange"] = 1000] = "AutoSnapshotBeforeWindowChange";
})(SettingKey_SettingKey || (SettingKey_SettingKey = {}));

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Shared/scripts/Classes/StaticHelpers.js







class StaticHelpers_StaticHelpers {
    static MinToMs(minutes) {
        return minutes * 60 * 1000;
    }
    static FlavorAsString(flavor) {
        return 'Flavor.' + SnapShotFlavor[flavor] + '(' + flavor + ')';
    }
    static SettingKeyAsString(settingKey) {
        return 'SettingKey.' + SettingKey_SettingKey[settingKey] + '(' + settingKey + ')';
    }
    static CacheModeAsString(cacheMode) {
        return 'CacheMode.' + CacheMode[cacheMode] + ' (' + cacheMode + ')';
    }
    static WindowTypeAsString(windowType) {
        return scWindowType[windowType];
    }
    static BufferString(str, desiredLength, buffCharEnum, direction) {
        var toReturn = str;
        var buffChar = ' ';
        if (buffCharEnum === BufferChar.space) {
            buffChar = ' ';
        }
        else if (buffCharEnum === BufferChar.Nbsp) {
            buffChar = '&nbsp;';
        }
        else if (buffCharEnum === BufferChar.Period) {
            buffChar = '.';
        }
        else if (buffCharEnum === BufferChar.Zero) {
            buffChar = '0';
        }
        if (toReturn.length > desiredLength) {
            if (desiredLength > 6) {
                toReturn = toReturn.substring(0, desiredLength - 3) + '...';
            }
            else {
                toReturn = toReturn.substring(0, desiredLength);
            }
        }
        if (toReturn.length < desiredLength) {
            var bufferCharNeeded = desiredLength - toReturn.length;
            for (var idx = 0; idx < bufferCharNeeded; idx++) {
                if (direction === BufferDirection.left) {
                    toReturn = buffChar + toReturn;
                }
                else {
                    toReturn = toReturn + buffChar;
                }
            }
        }
        return toReturn;
    }
    static MsgFlagAsString(msg) {
        var toReturn = "{error}";
        if (msg) {
            try {
                toReturn = 'MsgFlag.' + MsgFlag[msg] + '(' + msg + ')';
            }
            catch (e) {
            }
        }
        return 'flag: ' + toReturn;
    }
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Shared/scripts/Enums/scMode.js
var scMode_scMode;
(function (scMode) {
    scMode[scMode["Unknown"] = 0] = "Unknown";
    scMode[scMode["Edit"] = 1] = "Edit";
    scMode[scMode["Preview"] = 2] = "Preview";
    scMode[scMode["Normal"] = 3] = "Normal";
})(scMode_scMode || (scMode_scMode = {}));

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Shared/scripts/Interfaces/InjectConst.js

let InjectConst_ContentConst = (() => {
    class ContentConst {
    }
    ContentConst.Const = {
        MaxAutoToSaveCount: 10,
        MaxAutoSaveAgeDays: 1,
        MaxCacheAgeMs: 1000 * 60 * 10,
        Iterations: {
            MaxSetHrefEffort: 10,
            MaxIterationSwitchBoard: 20,
        },
        Timeouts: {
            PostLoginBtnClick: 1000,
            SetHrefEffortWait: 1000,
            WaitBeforeRemovingCompleteFlagOnContent: 1500,
            AutoSaveIntervalMin: 1,
        },
        ElemId: {
            sc: {
                scLoginUserName: 'UserName',
                scLoginPassword: 'Password',
                SitecoreRootNodeId: 'Tree_Node_11111111111111111111111111111111',
                SitecoreRootGlyphId: 'Tree_Glyph_11111111111111111111111111111111',
                scLoginBtn: {
                    sc920: 'LogInBtn',
                    sc820: null
                },
            }
        },
        ClassNames: {
            ContentTreeNode: 'scContentTreeNode',
            SC: {
                scContentTreeNodeActive: 'scContentTreeNodeActive',
            }
        },
        ScMode: {
            edit: { AsEnum: scMode_scMode.Edit, AsString: 'edit' },
            normal: { AsEnum: scMode_scMode.Normal, AsString: 'normal' },
            preview: { AsEnum: scMode_scMode.Preview, AsString: 'preview' },
        },
        Selector: {
            SC: {
                ContentTreeNodeGlyph: '.scContentTreeNodeGlyph',
                IdStartsWithTreeNode: '[id^=Tree_Node_]',
                IframeContent: {
                    sc820: null,
                    sc920: 'iframe[src*=Content]'
                },
                JqueryModalDialogsFrame: '[id=jqueryModalDialogsFrame]',
                ContentIframe0: '[id=scContentIframeId0]',
                NextButton: '[id=NextButton]',
                ContentIFrame1: '[id=scContentIframeId1]',
                Ok: '[id=OK]',
                Cancel: '[id=CancelButton]',
                MenuButtonPublish: '[id=B414550BADAF4542C9ADF44BED5FA6CB3E_menu_button]',
                MenuDropDownPublishItem: '[id=B414550BADAF4542C9ADF44BED5FA6CB3E_menu_98719A90225A4802A0625D3967E4DD47]',
                NavPublishStrip: '[id*=_Nav_PublishStrip]',
                StartMenuLeftOption: '.scStartMenuLeftOption',
                Publish: {
                    TheItemHasBeenPublished: '[id=LastPage]:not([style*=\'display:none\'])',
                    SettingsHidden: '[id=Settings][style*=\'display: none\']',
                    PublishingHidden: '[id=Publishing][style*=\'display: none\']',
                },
                LoginBtn: {
                    sc920: null,
                    sc820: 'input.btn',
                },
                scStartButton: {
                    sc920: '[id=StartButton]',
                    sc820: '[id=startButton]'
                },
            },
        },
        Storage: {
            SettingsSuffix: '.Settings',
            ShowDebugData: false,
            SnapShotPrefix: 'WindowSnapShot.',
            WindowRoot: 'HindSite.',
        },
        MaxIter: 100,
        MaxNullOrUndefinedIter: 100,
        prop: {
            AllTreeData: 'AllTreeData',
        },
        Names: {
            SC: {
                TreeGlyphPrefix: 'Tree_Glyph_',
                TreeNodePrefix: 'Tree_Node_',
                TreeExpandedPng: {
                    sc920: 'treemenu_expanded.png',
                    sc820: 'todo'
                }
            },
            PopUp: 'PopUp',
            PopUpStyles: 'PopUpStyles',
            TreeMenuExpandedPng: 'treemenu_expanded.png',
            TreeMenuCollapsedPng: 'treemenu_collapsed.png',
            scDefaultAdminPassword: 'b',
            scDefaultAdminUserName: 'admin',
        }
    };
    return ContentConst;
})();


// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Content/scripts/Managers/ContentAtticManager.js
var __awaiter = (undefined && undefined.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};







class ContentAtticManager_ContentAtticManager extends ContentManagerBase {
    constructor(hub, AllAgents) {
        super(hub, AllAgents);
        this.AllAgents.Logger.FuncStart(ContentAtticManager_ContentAtticManager.name);
        this.AllAgents.Logger.IsNotNullOrUndefinedBool("AllAgents.HelperAgent", this.AllAgents.HelperAgent);
        this.AllAgents.Logger.FuncEnd(ContentAtticManager_ContentAtticManager.name);
    }
    Init() {
    }
    UpdateNickname(payload) {
        return new Promise((resolve, reject) => __awaiter(this, void 0, void 0, function* () {
            this.AllAgents.Logger.FuncStart(this.UpdateNickname.name);
            var promResult = new PromiseResult(this.UpdateNickname.name, this.AllAgents.Logger);
            if (payload.IdOfSelect) {
                var storageMatch;
                yield this.GetFromStorageById(payload.IdOfSelect, CacheMode.OkToUseCache)
                    .then((result) => storageMatch = result);
                if (storageMatch && payload.SnapShotSettings && payload.SnapShotSettings.SnapShotNewNickname) {
                    storageMatch.NickName = payload.SnapShotSettings.SnapShotNewNickname;
                    this.WriteToStorage(storageMatch);
                    promResult.MarkSuccessful();
                }
                else {
                    promResult.MarkFailed('something was missing');
                }
            }
            else {
                promResult.MarkFailed('no payload or id');
                this.AllAgents.Logger.LogAsJsonPretty(this.UpdateNickname.name, payload);
            }
            this.AllAgents.Logger.FuncEnd(this.UpdateNickname);
            if (promResult.WasSuccessful()) {
                resolve();
            }
            else {
                reject(promResult.RejectReasons);
            }
        }));
    }
    MarkFavorite(data) {
        return new Promise((resolve, reject) => __awaiter(this, void 0, void 0, function* () {
            this.AllAgents.Logger.FuncStart(this.MarkFavorite.name);
            if (data.IdOfSelect) {
                yield this.GetFromStorageById(data.IdOfSelect, CacheMode.OkToUseCache)
                    .then((result) => {
                    result.Flavor = SnapShotFlavor.Favorite;
                    this.WriteToStorage(result);
                })
                    .then(() => resolve())
                    .catch((err) => { throw this.MarkFavorite.name + ' ' + err; });
            }
            else {
                reject('no data.idofselect');
            }
            this.AllAgents.Logger.FuncEnd(this.MarkFavorite.name);
        }));
    }
    WriteToStorage(dataOneWindow) {
        return __awaiter(this, void 0, void 0, function* () {
            return new Promise((resolve, reject) => __awaiter(this, void 0, void 0, function* () {
                this.AllAgents.Logger.FuncStart(this.WriteToStorage.name);
                var result = new PromiseResult(this.WriteToStorage.name, this.AllAgents.Logger);
                var snapShotAsString = JSON.stringify(dataOneWindow);
                yield window.localStorage.setItem(InjectConst_ContentConst.Const.Storage.WindowRoot + InjectConst_ContentConst.Const.Storage.SnapShotPrefix + dataOneWindow.Id.AsString, snapShotAsString);
                var foundInStorage = yield this.GetFromStorageById(dataOneWindow.Id, CacheMode.DoNotUseCach);
                if (foundInStorage) {
                    result.MarkSuccessful();
                }
                else {
                    result.MarkFailed('not found in storage');
                    result.MarkFailed('Snap shot not successfully saved');
                }
                this.AllAgents.Logger.FuncEnd(this.WriteToStorage.name);
                if (result.WasSuccessful()) {
                    resolve();
                }
                else {
                    reject(result.RejectReasons);
                }
            }));
        });
    }
    GetFromStorageById(needleId, cacheMode) {
        return __awaiter(this, void 0, void 0, function* () {
            return new Promise((resolve) => __awaiter(this, void 0, void 0, function* () {
                this.AllAgents.Logger.FuncStart(this.GetFromStorageById.name, needleId.AsString);
                var foundStorage;
                yield this.GetAllSnapShotsMany(cacheMode)
                    .then((result) => foundStorage = result);
                var DateOneWinStoreMatch = null;
                if (foundStorage) {
                    for (var idx = 0; idx < foundStorage.CurrentSnapShots.length; idx++) {
                        var candidate = foundStorage.CurrentSnapShots[idx];
                        if (candidate.Id.AsString === needleId.AsString) {
                            DateOneWinStoreMatch = candidate;
                            break;
                        }
                    }
                }
                if (DateOneWinStoreMatch) {
                    this.AllAgents.Logger.Log('found match', this.AllAgents.HelperAgent.UtilityHelp.TimeNicknameFavStr(DateOneWinStoreMatch));
                }
                else {
                    this.AllAgents.Logger.LogVal(this.GetFromStorageById.name, 'Match notfound');
                }
                this.AllAgents.Logger.FuncEnd(this.GetFromStorageById.name);
                resolve(DateOneWinStoreMatch);
            }));
        });
    }
    __parseRawData(oneRaw) {
        var candidate = JSON.parse(oneRaw.data);
        console.log;
        if (candidate) {
            candidate.TimeStamp = new Date(candidate.TimeStamp);
            candidate.Id = this.AllAgents.HelperAgent.GuidHelper.ParseGuid(candidate.Id.AsString);
            candidate.RawData = oneRaw;
            if (!candidate.WindowType) {
                candidate.WindowType = scWindowType.Unknown;
                candidate.WindowFriendly = scWindowType[candidate.WindowType];
            }
            if (!candidate.NickName) {
                candidate.NickName = '';
            }
        }
        else {
            this.AllAgents.Logger.ErrorAndThrow(this.__parseRawData.name, 'Saved data did not import correctly');
        }
        return candidate;
    }
    GetAllLocalStorageAsIOneStorageData() {
        return new Promise((resolve) => __awaiter(this, void 0, void 0, function* () {
            let prefix = InjectConst_ContentConst.Const.Storage.WindowRoot + InjectConst_ContentConst.Const.Storage.SnapShotPrefix;
            yield this.AllAgents.RepoAgent.GetBulkLocalStorageByKeyPrefix(prefix)
                .then((result) => resolve(result));
        }));
    }
    __getAllStorageReal() {
        return __awaiter(this, void 0, void 0, function* () {
            this.AllAgents.Logger.FuncStart(this.__getAllStorageReal.name);
            var toReturn = [];
            var rawStorageData;
            yield this.GetAllLocalStorageAsIOneStorageData()
                .then((result) => rawStorageData = result)
                .then(() => {
                if (rawStorageData) {
                    for (var idx = 0; idx < rawStorageData.length; idx++) {
                        toReturn.push(this.__parseRawData(rawStorageData[idx]));
                    }
                }
            })
                .then(() => {
                toReturn.sort((a, b) => +b.TimeStamp - +a.TimeStamp);
            })
                .then(() => toReturn = this.FilterOutOldData(toReturn));
            this.AllAgents.Logger.FuncEnd(this.__getAllStorageReal.name);
            return toReturn;
        });
    }
    CleanOutOldData() {
        this.AllAgents.Logger.FuncStart(this.CleanOutOldData.name);
        var cleanData = [];
        var now = new Date();
        var maxAutoSaveDiff = InjectConst_ContentConst.Const.MaxAutoSaveAgeDays * 24 * 60 * 60 * 1000;
        if (this.CachedWindowStorage) {
            var cacheLength = this.CachedWindowStorage.CurrentSnapShots.length;
            var autoCount = 0;
            for (var idx = 0; idx < cacheLength; idx++) {
                var deleteFlag = false;
                var candidate = this.CachedWindowStorage.CurrentSnapShots[idx];
                if (candidate.Flavor) {
                    if (autoCount > InjectConst_ContentConst.Const.MaxAutoToSaveCount) {
                        this.AllAgents.Logger.LogVal('Delete (max count :' + InjectConst_ContentConst.Const.MaxAutoToSaveCount + ')', candidate.TimeStamp.toString());
                        deleteFlag = true;
                    }
                    autoCount++;
                }
                if (now.getTime() - candidate.TimeStamp.getTime() > maxAutoSaveDiff) {
                    this.AllAgents.Logger.LogVal('Delete (Old : max' + InjectConst_ContentConst.Const.MaxAutoSaveAgeDays + ' days)', candidate.TimeStamp.toString());
                    deleteFlag = true;
                }
                if (!deleteFlag) {
                    cleanData.push(candidate);
                }
                else {
                    try {
                        this.AllAgents.Logger.LogVal('Cleaning old autosave', candidate.RawData.key);
                        window.localStorage.removeItem(candidate.RawData.key);
                    }
                    catch (e) {
                        this.AllAgents.Logger.ErrorAndThrow(this.CleanOutOldData.name, 'unable to delete key: ' + candidate.RawData.key);
                    }
                }
            }
            this.CachedWindowStorage.CurrentSnapShots = cleanData;
        }
        this.AllAgents.Logger.FuncEnd(this.CleanOutOldData.name);
    }
    GetAllSnapShotsMany(cacheMode) {
        return new Promise((resolve) => __awaiter(this, void 0, void 0, function* () {
            this.AllAgents.Logger.FuncStart(this.GetAllSnapShotsMany.name, StaticHelpers_StaticHelpers.CacheModeAsString(cacheMode));
            var toReturn;
            if (cacheMode === CacheMode.DoNotUseCach) {
                this.CachedWindowStorage = null;
            }
            if (!this.CachedWindowStorage) {
                this.CachedWindowStorage = {
                    CurrentSnapShots: [],
                    Birthday: new Date(1970),
                    FavoriteCount: 0,
                    SnapShotsAutoCount: 0,
                    PlainCount: 0,
                };
            }
            var timeDiff = Date.now() - this.CachedWindowStorage.Birthday.getTime();
            this.AllAgents.Logger.LogVal('cached timeDiff', timeDiff + ' : ' + InjectConst_ContentConst.Const.MaxCacheAgeMs);
            if (timeDiff > (InjectConst_ContentConst.Const.MaxCacheAgeMs)) {
                this.AllAgents.Logger.Log('updating cache');
                yield this.__getAllStorageReal()
                    .then((result) => this.CachedWindowStorage.CurrentSnapShots = result);
                this.CachedWindowStorage.Birthday = new Date();
            }
            else {
                this.AllAgents.Logger.Log('using cache');
            }
            this.CleanOutOldData();
            this.UpdateCounts();
            toReturn = this.CachedWindowStorage;
            resolve(toReturn);
            this.AllAgents.Logger.FuncEnd(this.GetAllSnapShotsMany.name);
        }));
    }
    UpdateCounts() {
        this.CachedWindowStorage.FavoriteCount = 0;
        this.CachedWindowStorage.SnapShotsAutoCount = 0;
        this.CachedWindowStorage.PlainCount = 0;
        for (var idx = 0; idx < this.CachedWindowStorage.CurrentSnapShots.length; idx++) {
            var candidate = this.CachedWindowStorage.CurrentSnapShots[idx];
            if (candidate.Flavor === SnapShotFlavor.Autosave) {
                this.CachedWindowStorage.SnapShotsAutoCount++;
            }
            else if (candidate.Flavor === SnapShotFlavor.Favorite) {
                this.CachedWindowStorage.FavoriteCount++;
            }
            else {
                this.CachedWindowStorage.PlainCount++;
            }
        }
    }
    FilterOutOldData(data) {
        var toReturn = data;
        return toReturn;
    }
    RemoveOneFromStorage(targetId) {
        return new Promise((resolve, reject) => __awaiter(this, void 0, void 0, function* () {
            this.AllAgents.Logger.FuncStart(this.RemoveOneFromStorage.name);
            var successful = true;
            var failMsg = '';
            try {
                if (targetId) {
                    var storageMatch = yield this.GetFromStorageById(targetId, CacheMode.OkToUseCache);
                    if (storageMatch) {
                        var result = confirm('Remove ?: ' + this.ContentHub.Utilities.TimeNicknameFavStrForConfirmation(storageMatch));
                        if (result === true) {
                            this.AllAgents.Logger.LogVal('Key to Delete', storageMatch.RawData.key);
                            yield window.localStorage.removeItem(storageMatch.RawData.key);
                            var stillExists = yield this.GetFromStorageById(targetId, CacheMode.DoNotUseCach);
                            if (stillExists) {
                                successful = false;
                                failMsg = 'Snapshot still exists after deleting';
                            }
                            else {
                                successful = true;
                            }
                            this.AllAgents.Logger.Log('Attempting completed');
                        }
                        else {
                            successful = false;
                            failMsg = 'Confirmation not received';
                        }
                    }
                    else {
                        successful = false;
                        failMsg = 'no storage match';
                    }
                }
                else {
                    successful = false;
                    failMsg = 'no target id';
                }
            }
            catch (e) {
                successful = false;
                failMsg = e.toString();
            }
            this.AllAgents.Logger.FuncEnd(this.RemoveOneFromStorage.name);
            if (successful) {
                this.AllAgents.Logger.Log('resolving');
                resolve();
            }
            else {
                this.AllAgents.Logger.Log('rejecting');
                reject(failMsg);
            }
        }));
    }
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Content/scripts/Managers/MiscManager.js


class MiscManager_MiscManager extends ContentManagerBase {
    constructor(hub, AllAgents) {
        super(hub, AllAgents);
        this.AllAgents.Logger.FuncStart(MiscManager_MiscManager.name);
        this.AllAgents.Logger.FuncEnd(MiscManager_MiscManager.name);
    }
    NotNullOrUndefined(subjectAnyOrAr, label = '', iterationCheck = null) {
        var toReturn = false;
        if (!iterationCheck) {
            iterationCheck = InjectConst_ContentConst.Const.MaxNullOrUndefinedIter;
        }
        iterationCheck -= 1;
        if (iterationCheck > 0) {
            if (label === '') {
                label = this.NotNullOrUndefined.name + ' : no label provided';
            }
            if (subjectAnyOrAr === 'undefined') {
                this.AllAgents.Logger.ErrorAndThrow(label, 'Is undefined');
            }
            else if (!subjectAnyOrAr) {
                this.AllAgents.Logger.ErrorAndThrow(label, 'Is Null');
            }
            else {
                this.AllAgents.Logger.LogVal(label, 'Passed');
                toReturn = true;
                if (Array.isArray(subjectAnyOrAr)) {
                    for (var idx = 0; idx < subjectAnyOrAr.length; idx++) {
                        toReturn = toReturn && this.NotNullOrUndefined(subjectAnyOrAr[idx], (idx + 1) + ':' + subjectAnyOrAr.length + ' ' + label, iterationCheck);
                    }
                }
            }
        }
        else {
            this.AllAgents.Logger.ErrorAndThrow(this.NotNullOrUndefined.name, 'max iteration hit');
        }
        this.AllAgents.Logger.LogVal('toReturn', toReturn);
        return toReturn;
    }
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Content/scripts/Promises/PromiseChainQuickPublish.js
var PromiseChainQuickPublish_awaiter = (undefined && undefined.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};



class PromiseChainQuickPublish_PromiseChainQuickPublish extends ContentManagerBase {
    constructor(hub, contentAgents) {
        super(hub, contentAgents);
    }
    __debugDataPublishChain(dataPublishChain, nickname) {
        this.AllAgents.Logger.FuncStart(this.__debugDataPublishChain.name, nickname);
        this.AllAgents.Logger.LogVal('docToPublish', this.AllAgents.Logger.IsNullOrUndefined(dataPublishChain.docToPublish));
        this.AllAgents.Logger.LogVal('jqIframe', this.AllAgents.Logger.IsNullOrUndefined(dataPublishChain.jqIframe) + ' ' + (dataPublishChain.jqIframe ? dataPublishChain.jqIframe.IframeElem.src : ''));
        this.AllAgents.Logger.LogVal('Iframe0blueIframe', this.AllAgents.Logger.IsNullOrUndefined(dataPublishChain.Iframe0Blue) + ' ' + (dataPublishChain.Iframe0Blue ? dataPublishChain.Iframe0Blue.IframeElem.src : ''));
        this.AllAgents.Logger.LogVal('messageDialogIframeRed', this.AllAgents.Logger.IsNullOrUndefined(dataPublishChain.messageDialogIframeRed) + ' ' + (dataPublishChain.messageDialogIframeRed ? dataPublishChain.messageDialogIframeRed.IframeElem.src : ''));
        this.AllAgents.Logger.FuncEnd(this.__debugDataPublishChain.name);
        return dataPublishChain;
    }
    PublishCE(docToPublish) {
        return PromiseChainQuickPublish_awaiter(this, void 0, void 0, function* () {
            this.AllAgents.Logger.FuncStart(this.PublishCE.name);
            var dataPublishChain = {
                docToPublish: docToPublish,
                TopLevelDoc: this.ScUiMan().TopLevelDoc(),
                Iframe0Blue: null,
                jqIframe: null,
                messageDialogIframeRed: null
            };
            yield this.PromiseOneStep().ClickPublishOnNav(dataPublishChain)
                .then((dataPublishChain) => this.PromiseOneStep().ClickMenuButtonPublishDropDown(dataPublishChain))
                .then((dataPublishChain) => this.PromiseOneStep().ClickMenuDropDownPublishItem(dataPublishChain))
                .then((dataPublishChain) => this.PromiseOneStep().GetThePublishItemDialog(dataPublishChain))
                .then((dataPublishChain) => this.GetDialogIframe0Blue(dataPublishChain))
                .then((dataPublishChain) => this.__WaitForAndClickPublishNextButton(dataPublishChain))
                .then((dataPublishChain) => this.GetMessageDialog(dataPublishChain))
                .then((dataPublishChain) => this.__waitForAndClickOk(dataPublishChain))
                .then((dataPublishChain) => this.__waitForAndClickClose(dataPublishChain))
                .catch(ex => {
                this.AllAgents.Logger.ErrorAndThrow(this.PublishCE.name, ex);
            });
            this.AllAgents.Logger.FuncEnd(this.PublishCE.name);
        });
    }
    __waitForAndClickClose(dataPublishChain) {
        return PromiseChainQuickPublish_awaiter(this, void 0, void 0, function* () {
            yield this.AllAgents.HelperAgent.PromisesBasic.WaitForAndReturnFoundElem(dataPublishChain.Iframe0Blue.ContentDoc, InjectConst_ContentConst.Const.Selector.SC.Publish.SettingsHidden)
                .then(() => PromiseChainQuickPublish_awaiter(this, void 0, void 0, function* () {
                yield this.AllAgents.HelperAgent.PromisesBasic.WaitForAndReturnFoundElem(dataPublishChain.Iframe0Blue.ContentDoc, InjectConst_ContentConst.Const.Selector.SC.Publish.TheItemHasBeenPublished, this.SharedConst().IterHelper.MaxCount.OverridePublishing);
            }))
                .then(() => PromiseChainQuickPublish_awaiter(this, void 0, void 0, function* () {
                yield this.AllAgents.HelperAgent.PromisesBasic.WaitForThenClick([InjectConst_ContentConst.Const.Selector.SC.Cancel], dataPublishChain.Iframe0Blue.ContentDoc);
            }));
            return dataPublishChain;
        });
    }
    __waitForAndClickOk(dataPublishChain) {
        return PromiseChainQuickPublish_awaiter(this, void 0, void 0, function* () {
            yield this.AllAgents.HelperAgent.PromisesBasic.WaitForThenClick([InjectConst_ContentConst.Const.Selector.SC.Ok], dataPublishChain.messageDialogIframeRed.ContentDoc);
            return dataPublishChain;
        });
    }
    __WaitForAndClickPublishNextButton(dataPublishChain) {
        return PromiseChainQuickPublish_awaiter(this, void 0, void 0, function* () {
            yield this.AllAgents.HelperAgent.PromisesBasic.WaitForThenClick([InjectConst_ContentConst.Const.Selector.SC.NextButton], dataPublishChain.Iframe0Blue.ContentDoc);
            return dataPublishChain;
        });
    }
    GetMessageDialog(dataPublishChain) {
        return PromiseChainQuickPublish_awaiter(this, void 0, void 0, function* () {
            let toReturnPublishChain = dataPublishChain;
            yield this.AllAgents.HelperAgent.PromisesBasic.WaitForIframeElemAndReturnWhenReady(dataPublishChain.jqIframe.ContentDoc, InjectConst_ContentConst.Const.Selector.SC.ContentIFrame1, 'iframeRed')
                .then((result) => toReturnPublishChain.messageDialogIframeRed = result)
                .catch((err) => this.AllAgents.Logger.ErrorAndThrow(this.GetMessageDialog.name, err));
            return toReturnPublishChain;
        });
    }
    GetDialogIframe0Blue(dataPublishChain = null) {
        return PromiseChainQuickPublish_awaiter(this, void 0, void 0, function* () {
            return new Promise((resolve, reject) => PromiseChainQuickPublish_awaiter(this, void 0, void 0, function* () {
                this.AllAgents.Logger.FuncStart(this.GetDialogIframe0Blue.name);
                let promiseResult = new PromiseResult(this.GetDialogIframe0Blue.name, this.AllAgents.Logger);
                this.AllAgents.Logger.LogAsJsonPretty('dataPublishChain', dataPublishChain);
                yield this.AllAgents.HelperAgent.PromisesBasic.WaitForIframeElemAndReturnWhenReady(dataPublishChain.jqIframe.ContentDoc, InjectConst_ContentConst.Const.Selector.SC.ContentIframe0, 'Iframe0Blue')
                    .then((result) => {
                    this.AllAgents.Logger.MarkerC();
                    dataPublishChain.Iframe0Blue = result;
                    promiseResult.MarkSuccessful();
                })
                    .catch((err) => promiseResult.MarkFailed(err));
                this.AllAgents.Logger.LogAsJsonPretty('dataPublishChain.Iframe0Blue', dataPublishChain.Iframe0Blue);
                this.AllAgents.Logger.FuncEnd(this.GetDialogIframe0Blue.name);
                if (promiseResult.WasSuccessful()) {
                    resolve(dataPublishChain);
                }
                else {
                    reject(promiseResult.RejectReasons);
                }
            }));
        });
    }
    __waitForThenFunc(selector, targetDoc, dataPublishChain, optionFunc) {
        return new Promise((resolve, reject) => PromiseChainQuickPublish_awaiter(this, void 0, void 0, function* () {
            this.AllAgents.Logger.FuncStart(this.__waitForThenFunc.name, selector);
            this.AllAgents.Logger.DebugIDataOneDoc(targetDoc);
            var found = null;
            yield this.AllAgents.HelperAgent.PromisesBasic.WaitForAndReturnFoundElem(targetDoc, selector)
                .then((result) => found = result);
            if (found) {
                this.AllAgents.Logger.Log('found');
                if (optionFunc) {
                    this.AllAgents.Logger.Log('executing func');
                    dataPublishChain = yield optionFunc(found, dataPublishChain);
                }
                this.__debugDataPublishChain(dataPublishChain, this.__waitForThenFunc.name);
                this.AllAgents.Logger.FuncEnd(this.__waitForThenFunc.name, selector);
                resolve(dataPublishChain);
            }
            else {
                reject('not found');
            }
        }));
    }
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Shared/scripts/SharedConst.js
let SharedConst = (() => {
    class SharedConst {
    }
    SharedConst.Const = {
        IterHelper: {
            GrowthPerIteration: 0.5,
            MaxCount: {
                Default: 10,
                OverridePublishing: 15,
            },
            Timeouts: {
                Max: 10000,
                Default: 100,
            },
        },
        Settings: {
            Defaults: {
                LogToConsole: true,
                LogToStorage: true
            }
        },
        Regex: {
            ContentEditor: /Content.*?Editor/ig,
            CleanGuid: /{|-|}/ig,
            NbSp: /&nbsp;/ig,
            QueryStrSeparatorQuest: /\?/gi,
            PageType: {
                Desktop: /.*default.aspx/ig,
                Edit: /sc_itemid=.*sc_mode=edit/ig,
                Normal: /sc_itemid=.*sc_mode=normal/ig,
                Preview: /sc_itemid=.*sc_mode=preview/ig,
            },
        },
        UrlSuffix: {
            Desktop: '/sitecore/shell/default.aspx',
            Login: '/sitecore/login',
            LaunchPad: '/client/applications/launchpad',
            CE: '/sitecore/shell/Applications/Content Editor.aspx?sc_bw=1',
            None: '/',
        },
    };
    return SharedConst;
})();


// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Shared/scripts/Agents/Drones/IterationDrone.js

class IterationDrone_IterationDrone {
    constructor(logger, nickname, maxIterations = null) {
        this.IsExhaustedMsg = 'Iteration helper exhausted';
        this.Logger = logger;
        if (!maxIterations) {
            maxIterations = SharedConst.Const.IterHelper.MaxCount.Default;
        }
        this.__maxIterations = maxIterations;
        this.__currentIteration = maxIterations;
        this.__timeout = SharedConst.Const.IterHelper.Timeouts.Default;
        this.__nickName = nickname;
        this.IsExhausted = false;
    }
    DecrementAndKeepGoing() {
        var toReturn = false;
        if (this.__currentIteration > 0) {
            this.__currentIteration -= 1;
            this.__timeout += this.__timeout * SharedConst.Const.IterHelper.GrowthPerIteration;
            if (this.__timeout > SharedConst.Const.IterHelper.Timeouts.Max) {
                this.__timeout = SharedConst.Const.IterHelper.Timeouts.Max;
            }
            this.Logger.Log('DecrementAndKeepGoing: ' + this.__nickName + ' ' + this.__currentIteration + ':' + this.__maxIterations + ' | timeout: ' + this.__timeout);
            toReturn = true;
        }
        else {
            this.IsExhausted = true;
            this.NotifyExhausted();
            toReturn = false;
        }
        return toReturn;
    }
    NotifyExhausted() {
        this.Logger.Log('Iteration: ' + this.__nickName + ' counter exhausted ' + this.__currentIteration + ':' + this.__maxIterations);
    }
    WaitAndThen(timeoutFunction) {
        this.Logger.FuncStart(this.WaitAndThen.name, this.__nickName + ' ' + timeoutFunction.name);
        var self = this;
        setTimeout(timeoutFunction(), self.__timeout);
        this.Logger.FuncEnd(this.WaitAndThen.name, this.__nickName);
    }
    Wait() {
        if (!this.OperationCancelled) {
            return new Promise((resolve) => {
                setTimeout(resolve, this.__timeout);
            });
        }
    }
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Content/scripts/Drones/OneTreeDrone.js

class OneTreeDrone_OneTreeDrone {
    constructor(logger, helperAgent, associatedDoc) {
        this.Logger = logger;
        this.HelperAgent = helperAgent;
        this.Logger.FuncStart(OneTreeDrone_OneTreeDrone.name);
        this.AssociatedDoc = associatedDoc;
        this.Logger.FuncEnd(OneTreeDrone_OneTreeDrone.name);
    }
    GetFriendlyNameFromNode(inputNode) {
        this.Logger.FuncStart(this.GetFriendlyNameFromNode.name);
        var toReturn = 'unknown';
        var parentNode = inputNode.parentNode;
        var treeNode = parentNode.querySelector(InjectConst_ContentConst.Const.Selector.SC.IdStartsWithTreeNode);
        if (treeNode) {
            toReturn = treeNode.innerText;
        }
        else {
            this.Logger.Log('No treeNode');
        }
        this.Logger.FuncEnd(this.GetFriendlyNameFromNode.name, toReturn);
        return toReturn;
    }
    __isActive(targetNode) {
        var toReturn = false;
        var firstNodeActiveTest = targetNode.querySelector(InjectConst_ContentConst.Const.Selector.SC.IdStartsWithTreeNode);
        if (firstNodeActiveTest) {
            var className = firstNodeActiveTest.className;
            if (className.indexOf(InjectConst_ContentConst.Const.ClassNames.SC.scContentTreeNodeActive) > -1) {
                toReturn = true;
                this.Logger.Log('** isActive ' + targetNode.innerText);
            }
        }
        return toReturn;
    }
    __isExpanded(firstImg) {
        var toReturn = false;
        if (firstImg) {
            var srcAttr = firstImg.getAttribute('src');
            if (srcAttr.indexOf(InjectConst_ContentConst.Const.Names.SC.TreeExpandedPng.sc920) > -1) {
                toReturn = true;
            }
            return toReturn;
        }
    }
    __isContentTreeNode(targetNode) {
        var toReturn = false;
        var className = targetNode.className;
        if (className === InjectConst_ContentConst.Const.ClassNames.ContentTreeNode) {
            toReturn = true;
        }
        return toReturn;
    }
    WalkNodeRecursive(targetNode, depth) {
        var toReturn = [];
        depth = depth - 1;
        if (targetNode) {
            var firstImg = targetNode.querySelector(InjectConst_ContentConst.Const.Selector.SC.ContentTreeNodeGlyph);
            if (this.__isContentTreeNode(targetNode)) {
                var newData = {
                    IsExpanded: this.__isExpanded(firstImg),
                    IsActive: this.__isActive(targetNode),
                    NodeFriendly: '',
                    NodeId: null
                };
                if (newData.IsExpanded || newData.IsActive) {
                    newData.NodeFriendly = this.GetFriendlyNameFromNode(firstImg);
                    var apparentId = firstImg.id.replace(InjectConst_ContentConst.Const.Names.SC.TreeGlyphPrefix, '');
                    newData.NodeId = this.HelperAgent.GuidHelper.ParseGuid(apparentId);
                    toReturn.push(newData);
                }
            }
            var childNodes = targetNode.children;
            if (childNodes && childNodes.length > 0 && depth > 0) {
                for (var jdx = 0; jdx < childNodes.length; jdx++) {
                    var oneChild = childNodes[jdx];
                    toReturn = toReturn.concat(this.WalkNodeRecursive(oneChild, depth));
                }
            }
        }
        return toReturn;
    }
    GetOneLiveTreeData() {
        this.Logger.FuncStart(this.GetOneLiveTreeData.name);
        this.Logger.Log('targetDoc isnull: ' + (this.AssociatedDoc === null));
        this.Logger.LogVal('Doc url: ', this.AssociatedDoc.ContentDoc.location.href);
        this.Logger.LogVal('Ready State: ', this.AssociatedDoc.ContentDoc.readyState);
        var toReturn = [];
        if (this.AssociatedDoc) {
            this.Logger.LogVal('Looking for node ID: ', InjectConst_ContentConst.Const.ElemId.sc.SitecoreRootNodeId);
            var rootNode = this.AssociatedDoc.ContentDoc.getElementById(InjectConst_ContentConst.Const.ElemId.sc.SitecoreRootNodeId);
            if (rootNode) {
                this.Logger.Log('rootNode: ' + rootNode.innerHTML);
                var rootParent = rootNode.parentElement;
                toReturn = this.WalkNodeRecursive(rootParent, InjectConst_ContentConst.Const.MaxIter);
                this.Logger.Log('foundNodes count: ' + toReturn.length);
            }
            else {
                this.Logger.ErrorAndThrow(this.GetOneLiveTreeData.name, 'no root node');
            }
        }
        else {
            this.Logger.ErrorAndThrow(this.GetOneLiveTreeData.name, 'no targetDoc');
        }
        this.Logger.FuncEnd(this.GetOneLiveTreeData.name);
        return toReturn;
    }
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Content/scripts/Managers/OneCEAgent.js
var OneCEAgent_awaiter = (undefined && undefined.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};




class OneCEAgent_OneCEAgent {
    constructor(associatedDoc, logger, helperAgent) {
        this.Logger = logger;
        this.HelperAgent = helperAgent;
        this.Logger.FuncStart(this.constructor.name);
        this.Logger.IsNotNullOrUndefinedBool("helperAgent", helperAgent);
        this.Logger.IsNotNullOrUndefinedBool("associatedDoc", associatedDoc);
        this.ContextDoc = associatedDoc;
        this.OneTreeDrone = new OneTreeDrone_OneTreeDrone(this.Logger, this.HelperAgent, this.ContextDoc);
        this.Logger.FuncEnd(this.constructor.name);
    }
    __activateNode(hotTreeNode) {
        this.Logger.FuncStart(this.__activateNode.name);
        this.Logger.Log('clicking it');
        hotTreeNode.click();
        this.Logger.FuncEnd(this.__activateNode.name);
    }
    __expandNode(foundOnPage) {
        this.Logger.FuncStart(this.__expandNode.name);
        var currentSrc = foundOnPage.getAttribute('src');
        this.Logger.Log('currentSrc' + currentSrc);
        if (currentSrc.indexOf(InjectConst_ContentConst.Const.Names.TreeMenuExpandedPng) < 0) {
            this.Logger.Log('clicking it');
            foundOnPage.click();
        }
        this.Logger.FuncEnd(this.__expandNode.name);
    }
    __collapseNode(element) {
        var currentSrc = element.getAttribute('src');
        this.Logger.Log('currentSrc' + currentSrc);
        if (currentSrc.indexOf(InjectConst_ContentConst.Const.Names.TreeMenuExpandedPng) > -1) {
            this.Logger.Log('clicking it');
            element.click();
        }
    }
    __collapseRootNode(targetCEDoc) {
        var rootElem = targetCEDoc.ContentDoc.getElementById(InjectConst_ContentConst.Const.ElemId.sc.SitecoreRootGlyphId);
        if (rootElem) {
            this.__collapseNode(rootElem);
        }
        else {
            this.Logger.ErrorAndThrow(this.__collapseRootNode.name, 'Root glyph not found ' + InjectConst_ContentConst.Const.ElemId.sc.SitecoreRootGlyphId);
        }
    }
    WaitForAndRestoreOneNode(nextNode, dataOneDocTarget) {
        return OneCEAgent_awaiter(this, void 0, void 0, function* () {
            this.Logger.FuncStart(this.WaitForAndRestoreOneNode.name, dataOneDocTarget.DocId.AsShort);
            var treeGlyphTargetId = InjectConst_ContentConst.Const.Names.SC.TreeGlyphPrefix + nextNode.NodeId.AsString;
            this.Logger.Log('looking for: ' + treeGlyphTargetId + ' ' + nextNode.NodeFriendly + ' in ' + dataOneDocTarget.DocId.AsShort);
            this.Logger.Log('document not null ' + (dataOneDocTarget.ContentDoc != null));
            var iterHelper = new IterationDrone_IterationDrone(this.Logger, this.WaitForAndRestoreOneNode.name);
            var foundOnPageTreeGlyph = null;
            while (!foundOnPageTreeGlyph && iterHelper.DecrementAndKeepGoing()) {
                this.Logger.Log('looking for: *' + treeGlyphTargetId + '* ' + nextNode.NodeFriendly + ' in *' + dataOneDocTarget.DocId.AsShort + '*');
                foundOnPageTreeGlyph = dataOneDocTarget.ContentDoc.getElementById(treeGlyphTargetId);
                if (foundOnPageTreeGlyph) {
                    if (nextNode.IsExpanded) {
                        this.__expandNode(foundOnPageTreeGlyph);
                    }
                    if (nextNode.IsActive) {
                        var hotTreeNodeId = InjectConst_ContentConst.Const.Names.SC.TreeNodePrefix + nextNode.NodeId.AsString;
                        var hotTreeNode = dataOneDocTarget.ContentDoc.getElementById(hotTreeNodeId);
                        if (hotTreeNode) {
                            this.__activateNode(hotTreeNode);
                        }
                    }
                }
                else {
                    this.Logger.Log('not Found...waiting: ');
                    yield iterHelper.Wait();
                }
            }
            this.Logger.FuncEnd(this.WaitForAndRestoreOneNode.name, dataOneDocTarget.DocId.AsShort);
        });
    }
    WaitForAndRestoreManyAllNodes(storageData, targetDoc) {
        return OneCEAgent_awaiter(this, void 0, void 0, function* () {
            this.Logger.FuncStart(this.WaitForAndRestoreManyAllNodes.name, targetDoc.DocId.AsShort);
            let iterHelper = new IterationDrone_IterationDrone(this.Logger, this.WaitForAndRestoreManyAllNodes.name);
            while (storageData.AllTreeNodeAr.length > 0 && iterHelper.DecrementAndKeepGoing()) {
                var nextNode = storageData.AllTreeNodeAr.shift();
                yield this.WaitForAndRestoreOneNode(nextNode, targetDoc);
            }
            this.Logger.FuncEnd(this.WaitForAndRestoreManyAllNodes.name);
        });
    }
    SetCompactCss() {
        this.Logger.FuncStart(this.SetCompactCss.name, this.ContextDoc.DocId.AsShort);
        this.Logger.FuncStart(this.SetCompactCss.name, this.ContextDoc.DocId.AsShort);
    }
    RestoreCEStateAsync(dataToRestore) {
        return OneCEAgent_awaiter(this, void 0, void 0, function* () {
            return new Promise((resolve, reject) => OneCEAgent_awaiter(this, void 0, void 0, function* () {
                this.Logger.FuncStart(this.RestoreCEStateAsync.name, this.ContextDoc.DocId.AsShort);
                var toReturn = false;
                this.Logger.Log('Node Count in storage data: ' + dataToRestore.AllTreeNodeAr.length);
                yield this.WaitForAndRestoreManyAllNodes(dataToRestore, this.ContextDoc)
                    .then(() => resolve(true))
                    .catch((err) => reject(this.RestoreCEStateAsync.name + " " + err));
                this.Logger.FuncEnd(this.RestoreCEStateAsync.name);
            }));
        });
    }
    GetActiveNode(allTreeNodeAr) {
        let toReturn = null;
        if (allTreeNodeAr) {
            for (var idx = 0; idx < allTreeNodeAr.length; idx++) {
                let candidate = allTreeNodeAr[idx];
                if (candidate.IsActive) {
                    toReturn = candidate;
                    break;
                }
            }
        }
        else {
            this.Logger.ErrorAndThrow(this.GetActiveNode.name, 'No tree data provided');
        }
        return toReturn;
    }
    GetTreeState(id) {
        return new Promise((resolve, reject) => {
            this.Logger.FuncStart(this.GetTreeState.name);
            let result = new PromiseResult(this.GetTreeState.name, this.Logger);
            var toReturnOneTreeState = {
                Id: id,
                AllTreeNodeAr: this.OneTreeDrone.GetOneLiveTreeData(),
                ActiveNode: null
            };
            toReturnOneTreeState.ActiveNode = this.GetActiveNode(toReturnOneTreeState.AllTreeNodeAr);
            if (toReturnOneTreeState) {
                result.MarkSuccessful();
            }
            else {
                result.MarkFailed('todo why would this fail?');
            }
            this.Logger.FuncEnd(this.GetTreeState.name);
            if (result.WasSuccessful()) {
                resolve(toReturnOneTreeState);
            }
            else {
                reject(result.RejectReasons);
            }
        });
    }
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Content/scripts/Promises/PromiseChainRestoreDesktop.js
var PromiseChainRestoreDesktop_awaiter = (undefined && undefined.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};



class PromiseChainRestoreDesktop_PromiseChainRestoreDesktop extends ContentManagerBase {
    constructor(hub, AllAgents) {
        super(hub, AllAgents);
        this.AllAgents.Logger.FuncStart(PromiseChainRestoreDesktop_PromiseChainRestoreDesktop.name);
        this.AllAgents.Logger.FuncEnd(PromiseChainRestoreDesktop_PromiseChainRestoreDesktop.name);
    }
    __waitForAndClickRedStartButton(targetDoc) {
        return new Promise((resolve, reject) => PromiseChainRestoreDesktop_awaiter(this, void 0, void 0, function* () {
            this.AllAgents.Logger.FuncStart(this.__waitForAndClickRedStartButton.name);
            if (this.MiscMan().NotNullOrUndefined(targetDoc, this.__waitForAndClickRedStartButton.name)) {
                this.AllAgents.Logger.MarkerB();
                yield this.AllAgents.HelperAgent.PromisesBasic.RaceWaitAndClick(InjectConst_ContentConst.Const.Selector.SC.scStartButton, targetDoc)
                    .then(() => resolve())
                    .catch((ex) => reject(ex));
            }
            else {
                reject(this.__waitForAndClickRedStartButton.name + ' something was null or undefined');
            }
            this.AllAgents.Logger.FuncEnd(this.__waitForAndClickRedStartButton.name);
        }));
    }
    __waitForIframeReady(targetIframe) {
        return new Promise((resolve, reject) => PromiseChainRestoreDesktop_awaiter(this, void 0, void 0, function* () {
            this.AllAgents.Logger.FuncStart(this.__waitForIframeReady.name, 'targetIframe not null: ' + (targetIframe !== null));
            yield this.AllAgents.HelperAgent.PromisesBasic.WaitForReadyIframe(targetIframe)
                .then((result) => {
                this.AllAgents.Logger.Log('resolved! : ');
                targetIframe.ContentDoc.ContentDoc = targetIframe.IframeElem.contentDocument;
                this.AllAgents.Logger.DebugDataOneIframe(targetIframe);
                resolve();
            })
                .catch((err) => {
                this.AllAgents.Logger.Log('rejected ! : ');
                reject(this.__waitForIframeReady.name + ' ' + err);
            });
            this.AllAgents.Logger.FuncEnd(this.__waitForIframeReady.name);
        }));
    }
    __waitForAndThenClickCEFromMenu(targetDoc) {
        return new Promise((resolve, reject) => PromiseChainRestoreDesktop_awaiter(this, void 0, void 0, function* () {
            yield this.AllAgents.HelperAgent.PromisesBasic.WaitForThenClick([InjectConst_ContentConst.Const.Selector.SC.StartMenuLeftOption], targetDoc)
                .then(() => { resolve(); })
                .catch((ex) => { reject(this.__waitForAndThenClickCEFromMenu.name + ' ' + ex); });
        }));
    }
    __restoreDataToOneIframe(oneTreeState, targetCeAgent) {
        return new Promise((resolve, reject) => PromiseChainRestoreDesktop_awaiter(this, void 0, void 0, function* () {
            this.AllAgents.Logger.FuncStart(this.__restoreDataToOneIframe.name);
            var success = yield this.OneScWinMan().OneDesktopMan.RestoreDataToOneIframeWorker(oneTreeState, targetCeAgent);
            if (success) {
                resolve();
            }
            else {
                reject(this.__restoreDataToOneIframe.name);
            }
            this.AllAgents.Logger.FuncEnd(this.__restoreDataToOneIframe.name);
        }));
    }
    RunOneChain(targetDoc, dataToRestore) {
        return PromiseChainRestoreDesktop_awaiter(this, void 0, void 0, function* () {
            this.AllAgents.Logger.FuncStart(this.RunOneChain.name);
            if (this.MiscMan().NotNullOrUndefined([targetDoc, dataToRestore], this.RunOneChain.name)) {
                var dataBucket = {
                    targetDoc: targetDoc,
                    IFramesbefore: allIframeDataAtBeginning,
                    oneTreeState: dataToRestore,
                    LastChainLinkSuccessful: false,
                };
                var allIframeDataAtBeginning;
                var targetCeAgent;
                yield this.AllAgents.HelperAgent.PromisesBasic.GetAllLiveIframeData(targetDoc)
                    .then((result) => allIframeDataAtBeginning = result)
                    .then(() => this.__waitForAndClickRedStartButton(dataBucket.targetDoc))
                    .then(() => this.__waitForAndThenClickCEFromMenu(dataBucket.targetDoc))
                    .then(() => this.AllAgents.HelperAgent.PromisesBasic.WaitForNewIframe(allIframeDataAtBeginning, dataBucket.targetDoc))
                    .then((result) => {
                    targetCeAgent = new OneCEAgent_OneCEAgent(result.ContentDoc, this.AllAgents.Logger, this.AllAgents.HelperAgent);
                    this.__waitForIframeReady(result);
                })
                    .then(() => this.__restoreDataToOneIframe(dataToRestore, targetCeAgent))
                    .catch(ex => {
                    this.AllAgents.Logger.ErrorAndThrow(this.RunOneChain.name, ex);
                });
                this.AllAgents.Logger.FuncEnd(this.RunOneChain.name);
            }
        });
    }
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Content/scripts/Managers/OneDesktopManager.js
var OneDesktopManager_awaiter = (undefined && undefined.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};



class OneDesktopManager_OneDesktopManager extends ContentManagerBase {
    constructor(hub, associatedDoc, AllAgents) {
        super(hub, AllAgents);
        this.AllAgents.Logger.FuncStart(OneDesktopManager_OneDesktopManager.name);
        this.associatedDoc = associatedDoc;
        this.AllAgents.Logger.FuncEnd(OneDesktopManager_OneDesktopManager.name);
    }
    RestoreDesktopState(targetDoc, dataToRestore) {
        return OneDesktopManager_awaiter(this, void 0, void 0, function* () {
            this.AllAgents.Logger.FuncStart(this.RestoreDesktopState.name);
            ;
            if (this.MiscMan().NotNullOrUndefined([targetDoc, dataToRestore, dataToRestore.AllCEAr], this.RestoreDesktopState.name)) {
                for (var idx = 0; idx < dataToRestore.AllCEAr.length; idx++) {
                    this.AllAgents.Logger.Log('data idx: ' + idx + ':' + dataToRestore.AllCEAr.length);
                    var desktopPromiser = new PromiseChainRestoreDesktop_PromiseChainRestoreDesktop(this.ContentHub, this.AllAgents);
                    yield desktopPromiser.RunOneChain(targetDoc, dataToRestore.AllCEAr[idx]);
                }
            }
            this.AllAgents.Logger.FuncEnd(this.RestoreDesktopState.name);
        });
    }
    RestoreDataToOneIframeWorker(oneTreeState, targetCeAgent) {
        return OneDesktopManager_awaiter(this, void 0, void 0, function* () {
            return new Promise((resolve, reject) => OneDesktopManager_awaiter(this, void 0, void 0, function* () {
                this.AllAgents.Logger.FuncStart(this.RestoreDataToOneIframeWorker.name, 'data not null: ' + (oneTreeState != null) + ' newFrame not null: ' + (targetCeAgent !== null));
                if (oneTreeState && targetCeAgent) {
                    yield targetCeAgent.RestoreCEStateAsync(oneTreeState)
                        .then(() => resolve(true))
                        .catch((err) => {
                        this.AllAgents.Logger.ErrorAndThrow(this.RestoreDataToOneIframeWorker.name, 'bad data');
                        reject((this.RestoreDataToOneIframeWorker.name + " " + err));
                    });
                }
                this.AllAgents.Logger.FuncEnd(this.RestoreDataToOneIframeWorker.name);
            }));
        });
    }
    GetStateDesktop() {
        return new Promise((resolve, reject) => OneDesktopManager_awaiter(this, void 0, void 0, function* () {
            this.AllAgents.Logger.FuncStart(this.GetStateDesktop.name);
            this.AllAgents.Logger.DebugIDataOneDoc(this.associatedDoc);
            var toReturnAllCeState = this.AllAgents.HelperAgent.FactoryHelp.CreateNewDtDataShell();
            yield this.AllAgents.HelperAgent.PromisesBasic.GetAllLiveIframeData(this.associatedDoc)
                .then((result) => toReturnAllCeState.livingIframeAr = result)
                .then(() => {
                if (toReturnAllCeState.livingIframeAr && toReturnAllCeState.livingIframeAr.length > 0) {
                    for (var iframeIdx = 0; iframeIdx < toReturnAllCeState.livingIframeAr.length; iframeIdx++) {
                        this.AllAgents.Logger.LogVal('iframeIdx: ', iframeIdx);
                        var targetIframeObj = toReturnAllCeState.livingIframeAr[iframeIdx];
                        var oneCeMan = new OneCEAgent_OneCEAgent(targetIframeObj.ContentDoc, this.AllAgents.Logger, this.AllAgents.HelperAgent);
                        oneCeMan.GetTreeState(this.AllAgents.HelperAgent.GuidHelper.NewGuid())
                            .then((oneCeState) => {
                            toReturnAllCeState.AllCeData.push(oneCeState);
                            if (targetIframeObj.Zindex === 1) {
                                toReturnAllCeState.ActiveCeMan = oneCeMan;
                                toReturnAllCeState.ActiveCeState = oneCeState;
                            }
                        })
                            .catch((err) => this.AllAgents.Logger.ErrorAndThrow(this.GetStateDesktop.name, err));
                    }
                }
            })
                .then(() => resolve(toReturnAllCeState))
                .catch((err) => reject(err));
            this.AllAgents.Logger.FuncEnd(this.GetStateDesktop.name);
        }));
    }
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Content/scripts/Managers/OneScWindowManager.js
var OneScWindowManager_awaiter = (undefined && undefined.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};






class OneScWindowManager_OneScWindowManager extends ContentManagerBase {
    constructor(hub, AllAgents) {
        super(hub, AllAgents);
        this.OneDesktopMan = null;
        this.OneCEAgent = null;
        this.AllAgents.Logger.FuncStart(OneScWindowManager_OneScWindowManager.name);
        this.AllAgents.Logger.FuncEnd(OneScWindowManager_OneScWindowManager.name);
    }
    Init() {
        let currPageType = this.ScUiMan().GetCurrentPageType();
        if (currPageType === scWindowType.Desktop) {
            this.OneDesktopMan = new OneDesktopManager_OneDesktopManager(this.ContentHub, this.ScUiMan().TopLevelDoc(), this.AllAgents);
        }
        else if (currPageType === scWindowType.ContentEditor) {
            this.OneCEAgent = new OneCEAgent_OneCEAgent(this.ScUiMan().TopLevelDoc(), this.AllAgents.Logger, this.AllAgents.HelperAgent);
        }
    }
    SaveWindowState(snapShotSettings) {
        return new Promise((resolve, reject) => OneScWindowManager_awaiter(this, void 0, void 0, function* () {
            this.AllAgents.Logger.FuncStart(this.SaveWindowState.name);
            let promiseResult = new PromiseResult(this.SaveWindowState.name, this.AllAgents.Logger);
            var snapShot = this.AllAgents.HelperAgent.FactoryHelp.CreateShellIDataOneWindowStorage(snapShotSettings.CurrentPageType, snapShotSettings.Flavor);
            if (snapShotSettings) {
                if (snapShotSettings.SnapShotNewNickname) {
                    snapShot.NickName = snapShotSettings.SnapShotNewNickname;
                }
                snapShot.Flavor = snapShotSettings.Flavor;
            }
            if (snapShotSettings.CurrentPageType === scWindowType.ContentEditor) {
                this.AllAgents.Logger.MarkerA();
                var id = this.AllAgents.HelperAgent.GuidHelper.EmptyGuid();
                yield this.OneCEAgent.GetTreeState(id)
                    .then((state) => {
                    snapShot.AllCEAr.push(state);
                    promiseResult.MarkSuccessful();
                })
                    .catch((err) => promiseResult.MarkFailed(err));
            }
            else if (snapShotSettings.CurrentPageType === scWindowType.Desktop) {
                this.AllAgents.Logger.MarkerB();
                yield this.OneDesktopMan.GetStateDesktop()
                    .then((states) => {
                    snapShot.AllCEAr = states.AllCeData;
                    promiseResult.MarkSuccessful();
                })
                    .catch((err) => promiseResult.MarkFailed(err));
            }
            else {
                this.AllAgents.Logger.ErrorAndThrow(this.SaveWindowState.name, 'Invalid page location ' + snapShotSettings.CurrentPageType);
            }
            this.AllAgents.Logger.FuncEnd(this.SaveWindowState.name);
            if (promiseResult.WasSuccessful) {
                yield this.AtticMan().WriteToStorage(snapShot)
                    .then(() => promiseResult.MarkSuccessful())
                    .catch((err) => promiseResult.MarkFailed(err));
            }
            if (promiseResult.WasSuccessful()) {
                resolve();
            }
            else {
                reject(promiseResult.RejectReasons);
            }
        }));
    }
    __getTopLevelIframe(targetDoc) {
        return OneScWindowManager_awaiter(this, void 0, void 0, function* () {
            return new Promise((resolve, reject) => OneScWindowManager_awaiter(this, void 0, void 0, function* () {
                var toReturn = null;
                var allIframe;
                yield this.AllAgents.HelperAgent.PromisesBasic.GetAllLiveIframeData(targetDoc)
                    .then((result) => {
                    allIframe = result;
                    var maxZVal = -1;
                    if (allIframe && allIframe.length > 0) {
                        for (var idx = 0; idx < allIframe.length; idx++) {
                            var candidateIframe = allIframe[idx];
                            if (candidateIframe && candidateIframe.Zindex > maxZVal) {
                                toReturn = candidateIframe;
                                maxZVal = candidateIframe.Zindex;
                            }
                        }
                    }
                })
                    .then(() => resolve(toReturn))
                    .catch((err) => this.AllAgents.Logger.ErrorAndThrow(this.__getTopLevelIframe.name, err));
            }));
        });
    }
    PublishActiveCE(targetDoc) {
        return OneScWindowManager_awaiter(this, void 0, void 0, function* () {
            this.AllAgents.Logger.FuncStart(this.PublishActiveCE.name);
            var currentWindowType = this.ScUiMan().GetCurrentPageType();
            var docToPublish = null;
            if (currentWindowType === scWindowType.Desktop) {
                var topIframe = yield this.__getTopLevelIframe(targetDoc);
                if (topIframe) {
                    docToPublish = topIframe.ContentDoc;
                }
            }
            else {
                docToPublish = this.ScUiMan().TopLevelDoc();
            }
            this.AllAgents.Logger.Log('docToPublish', this.AllAgents.Logger.IsNullOrUndefined(docToPublish));
            if (docToPublish) {
                var publishChain = new PromiseChainQuickPublish_PromiseChainQuickPublish(this.ContentHub, this.AllAgents);
                yield publishChain.PublishCE(docToPublish);
            }
            this.AllAgents.Logger.FuncEnd(this.PublishActiveCE.name);
        });
    }
    SetCompactCss(targetDoc) {
        return OneScWindowManager_awaiter(this, void 0, void 0, function* () {
            yield this.OneCEAgent.SetCompactCss();
        });
    }
    RestoreWindowStateToTargetDoc(targetDoc, dataToRestore) {
        return OneScWindowManager_awaiter(this, void 0, void 0, function* () {
            this.AllAgents.Logger.FuncStart(this.RestoreWindowStateToTargetDoc.name);
            if (dataToRestore) {
                if (dataToRestore.WindowType === scWindowType.ContentEditor) {
                    yield this.OneCEAgent.RestoreCEStateAsync(dataToRestore.AllCEAr[0]);
                }
                else if (dataToRestore.WindowType === scWindowType.Desktop) {
                    yield this.OneDesktopMan.RestoreDesktopState(targetDoc, dataToRestore);
                }
                else {
                    this.AllAgents.Logger.ErrorAndThrow(this.RestoreWindowStateToTargetDoc.name, 'No match found for snap shot');
                }
                this.AllAgents.Logger.FuncEnd(this.RestoreWindowStateToTargetDoc.name);
            }
        });
    }
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Shared/scripts/Classes/HelperBase.js
class HelperBase {
    constructor(logger, helperAgent) {
        this.Logger = logger;
        this.HelperAgent = helperAgent;
    }
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Shared/scripts/Classes/PromiseGeneric.js
var PromiseGeneric_awaiter = (undefined && undefined.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};




class PromiseGeneric_PromisesBasic extends HelperBase {
    WaitForReadyIframe(dataOneIframe) {
        return PromiseGeneric_awaiter(this, void 0, void 0, function* () {
            return new Promise((resolve, reject) => PromiseGeneric_awaiter(this, void 0, void 0, function* () {
                this.Logger.FuncStart(this.WaitForReadyIframe.name, dataOneIframe.Nickname + ' ' + dataOneIframe.Id.AsShort);
                var iterationJr = new IterationDrone_IterationDrone(this.Logger, this.WaitForReadyIframe.name);
                let promiseResult = new PromiseResult(this.WaitForReadyIframe.name, this.Logger);
                while (iterationJr.DecrementAndKeepGoing() && !promiseResult.WasSuccessful()) {
                    var currentReadyState = dataOneIframe.IframeElem.contentDocument.readyState.toString();
                    var isReadyStateComplete = currentReadyState === 'complete';
                    var currentDocName = dataOneIframe.IframeElem.contentDocument.location.href;
                    if (isReadyStateComplete && (currentDocName !== 'about:blank')) {
                        this.Logger.LogVal('doc name', dataOneIframe.IframeElem.contentDocument.location.href);
                        promiseResult.MarkSuccessful();
                    }
                    else {
                        yield iterationJr.Wait();
                    }
                }
                if (iterationJr.IsExhausted) {
                    promiseResult.MarkFailed(iterationJr.IsExhaustedMsg);
                }
                if (promiseResult.WasSuccessful()) {
                    resolve();
                }
                else {
                    reject(promiseResult.RejectReasons);
                }
                this.Logger.FuncEnd(this.WaitForReadyIframe.name);
            }));
        });
    }
    WaitForPageReadyNative(targetDoc) {
        return PromiseGeneric_awaiter(this, void 0, void 0, function* () {
            return new Promise((resolve, reject) => PromiseGeneric_awaiter(this, void 0, void 0, function* () {
                this.Logger.FuncStart(this.WaitForPageReadyNative.name);
                var result = new PromiseResult(this.WaitForPageReadyNative.name, this.Logger);
                this.Logger.DebugIDataOneDoc(targetDoc);
                var iterationJr = new IterationDrone_IterationDrone(this.Logger, this.WaitForPageReadyNative.name);
                var isReady = false;
                this.Logger.MarkerA();
                while (iterationJr.DecrementAndKeepGoing() && !isReady) {
                    this.Logger.MarkerB();
                    var currentReadyState = targetDoc.ContentDoc.readyState.toString();
                    var isReadyStateComplete = currentReadyState === 'complete';
                    this.Logger.LogVal('readyState', currentReadyState);
                    ;
                    this.Logger.LogVal('isReadyStateComplete', isReadyStateComplete);
                    if (isReadyStateComplete) {
                        isReady = true;
                        result.MarkSuccessful();
                    }
                    else {
                        yield iterationJr.Wait();
                    }
                }
                if (iterationJr.IsExhausted) {
                    result.MarkFailed(iterationJr.IsExhaustedMsg);
                }
                this.Logger.FuncEnd(this.WaitForPageReadyNative.name, 'ready state: ' + currentReadyState + ' is ready: ' + isReady.toString());
                ;
                if (result.WasSuccessful()) {
                    resolve();
                }
                else {
                    reject(result.RejectReasons);
                }
            }));
        });
    }
    WaitForIframeElemAndReturnWhenReady(haystackDoc, selector, iframeNickName) {
        return PromiseGeneric_awaiter(this, void 0, void 0, function* () {
            return new Promise((resolve, reject) => PromiseGeneric_awaiter(this, void 0, void 0, function* () {
                this.Logger.FuncStart(this.WaitForIframeElemAndReturnWhenReady.name);
                var toReturnIframeData = null;
                let promiseResult = new PromiseResult(this.WaitForIframeElemAndReturnWhenReady.name, this.Logger);
                yield this.WaitForAndReturnFoundElem(haystackDoc, selector)
                    .then((foundElem) => PromiseGeneric_awaiter(this, void 0, void 0, function* () {
                    if (foundElem) {
                        toReturnIframeData = this.HelperAgent.FactoryHelp.DataOneIframeFactory(foundElem, iframeNickName);
                    }
                }))
                    .then(() => this.WaitForReadyIframe(toReturnIframeData))
                    .then(() => {
                    toReturnIframeData.ContentDoc = this.HelperAgent.FactoryHelp.DataOneContentDocFactoryFromIframe(toReturnIframeData);
                    promiseResult.MarkSuccessful();
                })
                    .catch((err) => promiseResult.MarkFailed(err));
                this.Logger.FuncEnd(this.WaitForIframeElemAndReturnWhenReady.name);
                if (promiseResult.WasSuccessful()) {
                    resolve(toReturnIframeData);
                }
                else {
                    reject(promiseResult.RejectReasons);
                }
            }));
        });
    }
    GetAllLiveIframeData(targetDoc) {
        return new Promise((resolve, reject) => {
            this.Logger.FuncStart(this.GetAllLiveIframeData.name);
            let successful = true;
            let rejectReason = '';
            var toReturn = [];
            var iframeAr = targetDoc.ContentDoc.querySelectorAll(InjectConst_ContentConst.Const.Selector.SC.IframeContent.sc920);
            if (!iframeAr) {
                iframeAr = targetDoc.ContentDoc.querySelectorAll(InjectConst_ContentConst.Const.Selector.SC.IframeContent.sc820);
            }
            this.Logger.LogVal('found iframes count', iframeAr.length);
            if (iframeAr) {
                for (var ifrIdx = 0; ifrIdx < iframeAr.length; ifrIdx++) {
                    this.Logger.Log('pushing: ' + ifrIdx);
                    var iframeElem = iframeAr[ifrIdx];
                    var dataOneIframe = this.HelperAgent.FactoryHelp.DataOneIframeFactory(iframeElem, 'desktop Iframe_' + ifrIdx);
                    toReturn.push(dataOneIframe);
                }
            }
            else {
                successful = false;
                rejectReason = 'no iframes found';
                this.Logger.Log(rejectReason);
            }
            this.Logger.LogVal('GetAllLiveIframeData: iframe count', toReturn.length);
            if (successful) {
                resolve(toReturn);
            }
            else {
                reject(rejectReason);
            }
            this.Logger.FuncEnd(this.GetAllLiveIframeData.name);
        });
    }
    WaitForNewIframe(allIframesBefore, targetDoc) {
        return PromiseGeneric_awaiter(this, void 0, void 0, function* () {
            return new Promise((resolve, reject) => PromiseGeneric_awaiter(this, void 0, void 0, function* () {
                this.Logger.FuncStart(this.WaitForNewIframe.name);
                this.Logger.LogAsJsonPretty('allIframesBefore', allIframesBefore);
                this.Logger.ThrowIfNullOrUndefined(this.WaitForNewIframe.name, allIframesBefore);
                this.Logger.ThrowIfNullOrUndefined(this.WaitForNewIframe.name, targetDoc);
                var toReturn = null;
                var iterationJr = new IterationDrone_IterationDrone(this.Logger, this.WaitForNewIframe.name);
                let beforeCount = allIframesBefore.length;
                while (!toReturn && iterationJr.DecrementAndKeepGoing()) {
                    var allIframesAfter;
                    yield this.GetAllLiveIframeData(targetDoc)
                        .then((result) => allIframesAfter = result)
                        .catch((err) => this.Logger.ErrorAndThrow(this.WaitForNewIframe.name, err));
                    var count = allIframesAfter.length;
                    this.Logger.Log('iFrame count before: ' + beforeCount);
                    this.Logger.Log('iFrame count after: ' + allIframesAfter.length);
                    if (count > beforeCount) {
                        var newIframes = allIframesAfter.filter(e => !allIframesBefore.includes(e));
                        toReturn = newIframes[0];
                    }
                    else {
                        yield iterationJr.Wait();
                    }
                }
                this.Logger.FuncEnd(this.WaitForNewIframe.name);
                if (toReturn) {
                    resolve(toReturn);
                }
                else {
                    reject(iterationJr.IsExhaustedMsg);
                }
            }));
        });
    }
    WaitForAndReturnFoundElem(haystackDoc, selector, overrideIterCount = 8) {
        return PromiseGeneric_awaiter(this, void 0, void 0, function* () {
            return new Promise((resolve, reject) => PromiseGeneric_awaiter(this, void 0, void 0, function* () {
                this.Logger.FuncStart(this.WaitForAndReturnFoundElem.name);
                this.Logger.LogVal('selector', selector);
                this.Logger.LogVal('doc nickname', haystackDoc.Nickname);
                var toReturnFoundElem = null;
                let promiseResult = new PromiseResult(this.WaitForAndReturnFoundElem.name, this.Logger);
                var iterationJr = new IterationDrone_IterationDrone(this.Logger, this.WaitForAndReturnFoundElem.name, overrideIterCount);
                while (!toReturnFoundElem && iterationJr.DecrementAndKeepGoing()) {
                    this.Logger.LogVal('targetDoc.Document', haystackDoc.ContentDoc.toString());
                    this.Logger.LogVal('targetDoc.Document.location', haystackDoc.ContentDoc.location.toString());
                    this.Logger.LogVal('targetDoc.Document.location.href', haystackDoc.ContentDoc.location.href);
                    toReturnFoundElem = haystackDoc.ContentDoc.querySelector(selector);
                    if (toReturnFoundElem) {
                        this.Logger.Log('found');
                        this.Logger.LogVal('found.style.display', toReturnFoundElem.style.display);
                        promiseResult.MarkSuccessful();
                    }
                    else {
                        yield iterationJr.Wait();
                    }
                }
                if (!toReturnFoundElem && iterationJr.IsExhausted) {
                    promiseResult.MarkFailed(iterationJr.IsExhaustedMsg);
                }
                this.Logger.FuncEnd(this.WaitForAndReturnFoundElem.name);
                if (promiseResult.WasSuccessful()) {
                    resolve(toReturnFoundElem);
                }
                else {
                    reject(promiseResult.RejectReasons);
                }
            }));
        });
    }
    WaitForAndClickWithPayload(selector, targetDoc, payload) {
        return new Promise((resolve, reject) => PromiseGeneric_awaiter(this, void 0, void 0, function* () {
            this.Logger.FuncStart(this.WaitForAndClickWithPayload.name, selector);
            yield this.WaitForThenClick([selector], targetDoc)
                .then(() => resolve(payload))
                .catch(ex => {
                this.Logger.ErrorAndThrow(this.WaitForAndClickWithPayload.name, ex);
                reject(ex);
            });
        }));
    }
    TabWaitForReadyStateCompleteNative(browserTab) {
        return new Promise((resolve, reject) => PromiseGeneric_awaiter(this, void 0, void 0, function* () {
            let iterHelper = new IterationDrone_IterationDrone(this.Logger, this.TabWaitForReadyStateCompleteNative.name);
            let result = new PromiseResult(this.TabWaitForReadyStateCompleteNative.name, this.Logger);
            while (browserTab.status !== 'complete' && iterHelper.DecrementAndKeepGoing()) {
                this.Logger.LogVal('tab status', browserTab.status);
                yield iterHelper.Wait;
            }
            if (browserTab.status === 'complete') {
                result.MarkSuccessful();
            }
            else {
                result.MarkFailed('browser status: ' + browserTab.status);
                if (iterHelper.IsExhausted) {
                    result.MarkFailed(iterHelper.IsExhaustedMsg);
                }
            }
            if (result.WasSuccessful()) {
                resolve();
            }
            else {
                reject(result.RejectReasons);
            }
        }));
    }
    TabChainSetHrefWaitForComplete(href, targetTab) {
        return new Promise((resolve, reject) => PromiseGeneric_awaiter(this, void 0, void 0, function* () {
            this.Logger.FuncStart(this.TabChainSetHrefWaitForComplete.name, href.AbsUrl);
            try {
                yield browser.tabs.update(targetTab.Tab.id, { url: href.AbsUrl })
                    .then(() => this.TabWaitForReadyStateCompleteNative(targetTab.Tab))
                    .then(resolve)
                    .catch(reject);
            }
            catch (e) {
                this.Logger.ErrorAndThrow(this.TabChainSetHrefWaitForComplete.name, e.toString());
                reject(e);
            }
            this.Logger.FuncEnd(this.TabChainSetHrefWaitForComplete.name, href.AbsUrl);
        }));
    }
    RaceWaitAndClick(selector, targetDoc) {
        return PromiseGeneric_awaiter(this, void 0, void 0, function* () {
            return new Promise((resolve, reject) => PromiseGeneric_awaiter(this, void 0, void 0, function* () {
                this.Logger.FuncStart(this.RaceWaitAndClick.name);
                yield this.WaitForThenClick([selector.sc920, selector.sc820], targetDoc)
                    .then(() => {
                    this.Logger.FuncEnd(this.RaceWaitAndClick.name);
                    resolve();
                })
                    .catch((ex) => {
                    this.Logger.FuncEnd(this.RaceWaitAndClick.name);
                    reject(ex);
                });
            }));
        });
    }
    WaitForThenClick(selectorAr, targetDoc) {
        return new Promise((resolve, reject) => PromiseGeneric_awaiter(this, void 0, void 0, function* () {
            this.Logger.FuncStart(this.WaitForThenClick.name);
            let promiseResults = new PromiseResult(this.WaitForThenClick.name, this.Logger);
            if (targetDoc) {
                this.Logger.LogAsJsonPretty('selectors', selectorAr);
                var found = null;
                var iterationJr = new IterationDrone_IterationDrone(this.Logger, this.WaitForThenClick.name);
                while (!found && iterationJr.DecrementAndKeepGoing()) {
                    for (var idx = 0; idx < selectorAr.length; idx++) {
                        found = targetDoc.ContentDoc.querySelector(selectorAr[idx]);
                        if (found) {
                            this.Logger.LogVal('found target', selectorAr[idx]);
                            break;
                        }
                    }
                    if (found) {
                        this.Logger.Log('clicking');
                        try {
                            found.click();
                            promiseResults.MarkSuccessful();
                        }
                        catch (e) {
                            promiseResults.MarkFailed(e);
                        }
                    }
                    else {
                        yield iterationJr.Wait();
                    }
                }
            }
            else {
                promiseResults.MarkFailed('no target doc');
            }
            if (!found && iterationJr.IsExhausted) {
                promiseResults.MarkFailed(iterationJr.IsExhaustedMsg);
            }
            this.Logger.FuncEnd(this.WaitForThenClick.name);
            if (promiseResults.WasSuccessful()) {
                resolve();
            }
            else {
                reject(promiseResults.RejectReasons);
            }
        }));
    }
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Content/scripts/Promises/PromiseOneStep.js
var PromiseOneStep_awaiter = (undefined && undefined.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};



class PromiseOneStep_PromiseOneStep extends ContentManagerBase {
    constructor(hub, AllAgents) {
        super(hub, AllAgents);
        this.AllAgents.Logger.FuncStart(PromiseGeneric_PromisesBasic.name);
        this.AllAgents.Logger.FuncEnd(PromiseGeneric_PromisesBasic.name);
    }
    ClickPublishOnNav(payload) {
        return PromiseOneStep_awaiter(this, void 0, void 0, function* () {
            yield this.AllAgents.HelperAgent.PromisesBasic.WaitForThenClick([InjectConst_ContentConst.Const.Selector.SC.NavPublishStrip], payload.docToPublish);
            yield this.AllAgents.HelperAgent.PromisesBasic.WaitForThenClick([InjectConst_ContentConst.Const.Selector.SC.NavPublishStrip], payload.docToPublish);
            return payload;
        });
    }
    ClickMenuButtonPublishDropDown(payload = null) {
        return PromiseOneStep_awaiter(this, void 0, void 0, function* () {
            yield this.AllAgents.HelperAgent.PromisesBasic.WaitForThenClick([InjectConst_ContentConst.Const.Selector.SC.MenuButtonPublish], payload.docToPublish);
            return payload;
        });
    }
    ClickMenuDropDownPublishItem(payload = null) {
        return PromiseOneStep_awaiter(this, void 0, void 0, function* () {
            return yield this.AllAgents.HelperAgent.PromisesBasic.WaitForAndClickWithPayload(InjectConst_ContentConst.Const.Selector.SC.MenuDropDownPublishItem, payload.docToPublish, payload);
        });
    }
    GetThePublishItemDialog(dataPublishChain = null) {
        return PromiseOneStep_awaiter(this, void 0, void 0, function* () {
            yield this.AllAgents.HelperAgent.PromisesBasic.WaitForAndReturnFoundElem(dataPublishChain.TopLevelDoc, InjectConst_ContentConst.Const.Selector.SC.JqueryModalDialogsFrame)
                .then((found) => {
                dataPublishChain.jqIframe = this.AllAgents.HelperAgent.FactoryHelp.DataOneIframeFactory(found, 'jqIframe');
                return dataPublishChain;
            })
                .then((payload) => PromiseOneStep_awaiter(this, void 0, void 0, function* () {
                yield this.AllAgents.HelperAgent.PromisesBasic.WaitForReadyIframe(payload.jqIframe);
                dataPublishChain = payload;
            }));
            return dataPublishChain;
        });
    }
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Shared/scripts/Classes/PayloadDataFromContent.js
class PayloadDataFromContent {
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Shared/scripts/Interfaces/MsgFromXBase.js

class MsgFromXBase_MsgFromXBase {
    constructor(msgFlag) {
        this.MsgFlag = MsgFlag.Unknown;
        this.MsgFlag = msgFlag;
    }
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Shared/scripts/Classes/MsgPayloadResponseFromContent.js



class MsgPayloadResponseFromContent_MsgFromContent extends MsgFromXBase_MsgFromXBase {
    constructor(msgFlag) {
        super(msgFlag);
        this.Data = new PayloadDataFromContent();
        this.ContentState = {
            SnapShotsMany: {
                CurrentSnapShots: [],
                FavoriteCount: 0,
                SnapShotsAutoCount: 0,
                PlainCount: 0,
                Birthday: new Date(1970),
            },
            LastReq: MsgFlag.Unknown,
            ErrorStack: [],
            LastReqSuccessful: false,
            LastReqFailReason: '',
            ActiveCe: null
        };
    }
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Shared/scripts/Classes/PayloadDataReqPopUp.js
class PayloadDataFromPopUp {
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Content/scripts/Managers/ContentMessageManager.js
var ContentMessageManager_awaiter = (undefined && undefined.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};











class ContentMessageManager_ContentMessageManager extends ContentManagerBase {
    constructor(contentHub, contentAgents) {
        super(contentHub, contentAgents);
        this.AutoSaveHasBeenScheduled = false;
        this.AllAgents.Logger.FuncStart(ContentMessageManager_ContentMessageManager.name);
        var self = this;
        browser.runtime.onMessage.addListener(request => self.ContentReceiveRequest(request));
        this.AllAgents.Logger.FuncEnd(ContentMessageManager_ContentMessageManager.name);
    }
    ValidateRequest(reqMsgFromPopup) {
        this.AllAgents.Logger.FuncStart(this.ValidateRequest.name);
        var isValid = true;
        if (reqMsgFromPopup) {
            if (reqMsgFromPopup.CurrentContentPrefs) {
                if (reqMsgFromPopup.Data) {
                }
                else {
                    reqMsgFromPopup.Data = new PayloadDataFromPopUp();
                }
            }
            else {
                this.AllAgents.Logger.ErrorAndThrow(this.ValidateRequest.name, 'No CurrentContentPrefs');
                reqMsgFromPopup.IsValid = false;
                isValid = false;
            }
        }
        else {
            this.AllAgents.Logger.ErrorAndThrow(this.ValidateRequest.name, 'no reqMsgFromPopup');
        }
        reqMsgFromPopup.IsValid = isValid;
        this.AllAgents.Logger.FuncEnd(this.ValidateRequest.name, isValid.toString());
        return reqMsgFromPopup;
    }
    ScheduleIntervalTasks(reqMsgFromPopup) {
        this.AllAgents.Logger.FuncStart(this.ScheduleIntervalTasks.name);
        this.AllAgents.Logger.LogVal('Has been scheduled: ', this.AutoSaveHasBeenScheduled);
        let autoSaveSetting = this.AllAgents.SettingsAgent.GetByKey(SettingKey_SettingKey.AutoSaveIntervalMin);
        if (this.AllAgents.SettingsAgent.ValueAsInteger(autoSaveSetting) > 0) {
            if (!this.AutoSaveHasBeenScheduled) {
                this.AllAgents.Logger.MarkerA();
                var self = this;
                this.AllAgents.Logger.MarkerB();
                var intervalMs = StaticHelpers_StaticHelpers.MinToMs(InjectConst_ContentConst.Const.Timeouts.AutoSaveIntervalMin);
                this.AllAgents.Logger.MarkerC();
                window.setInterval(() => {
                    self.AutoSaveSnapShot(this.ScUiMan().GetCurrentPageType());
                }, intervalMs);
                this.AllAgents.Logger.MarkerD();
                this.AutoSaveHasBeenScheduled = true;
            }
        }
        this.AllAgents.Logger.FuncEnd(this.ScheduleIntervalTasks.name);
    }
    AutoSaveSnapShot(pageType) {
        this.AllAgents.Logger.FuncStart(this.AutoSaveSnapShot.name);
        var SnapShotSettings = {
            SnapShotNewNickname: '',
            Flavor: SnapShotFlavor.Autosave,
            CurrentPageType: pageType
        };
        this.OneScWinMan().SaveWindowState(SnapShotSettings);
        this.AllAgents.Logger.FuncEnd(this.AutoSaveSnapShot.name);
    }
    SetLoggerFromMessage(reqMsgFromPopup) {
        return ContentMessageManager_awaiter(this, void 0, void 0, function* () {
            let currSetting = this.AllAgents.SettingsAgent.GetByKey(SettingKey_SettingKey.LogToConsole);
            let valueToUse = SharedConst.Const.Settings.Defaults.LogToConsole;
            if (currSetting) {
                let candidate = this.AllAgents.SettingsAgent.ValueAsBool(currSetting);
                if (candidate) {
                    console.log('setting value as bool ' + valueToUse);
                    console.log('setting it to ' + valueToUse);
                }
                else {
                    console.log('candidate was null');
                }
                yield this.AllAgents.Logger.Init(valueToUse);
            }
            else {
                console.log('curr setting not found');
            }
        });
    }
    ContentReceiveRequest(reqMsgFromPopup) {
        return ContentMessageManager_awaiter(this, void 0, void 0, function* () {
            this.AllAgents.SettingsAgent.SetContentSettings(reqMsgFromPopup.CurrentContentPrefs);
            return new Promise((resolve, reject) => ContentMessageManager_awaiter(this, void 0, void 0, function* () {
                this.AllAgents.Logger.FuncStart(this.ContentReceiveRequest.name, StaticHelpers_StaticHelpers.MsgFlagAsString(reqMsgFromPopup.MsgFlag));
                var promResult = new PromiseResult(this.ContentReceiveRequest.name, this.AllAgents.Logger);
                var response = null;
                if (reqMsgFromPopup) {
                    reqMsgFromPopup = this.ValidateRequest(reqMsgFromPopup);
                    if (reqMsgFromPopup.IsValid) {
                        yield this.ReqMsgRouter(reqMsgFromPopup)
                            .then((result) => { response = result; })
                            .then(() => promResult.MarkSuccessful())
                            .catch((err) => promResult.MarkFailed(err));
                    }
                    else {
                        promResult.MarkFailed('reqMsgFromPopup is not valid');
                    }
                }
                else {
                    promResult.MarkFailed('no request');
                }
                if (response !== null) {
                    this.AllAgents.Logger.LogVal('responding', StaticHelpers_StaticHelpers.MsgFlagAsString(response.MsgFlag));
                }
                if (promResult.WasSuccessful()) {
                    resolve(response);
                }
                else {
                    response = new MsgPayloadResponseFromContent_MsgFromContent(MsgFlag.RespError);
                    response.ContentState.LastReqFailReason = promResult.RejectReasons;
                    reject(promResult.RejectReasons);
                }
                this.AllAgents.Logger.FuncEnd(this.ContentReceiveRequest.name, StaticHelpers_StaticHelpers.MsgFlagAsString(reqMsgFromPopup.MsgFlag));
            }));
        });
    }
    Init() {
        this.AllAgents.Logger.FuncStart(this.Init.name + ' ' + ContentMessageManager_ContentMessageManager.name);
        var self = this;
        this.AllAgents.Logger.FuncEnd(this.Init.name);
    }
    NotifyCompleteOnContent(targetDoc = null, Message) {
        if (!targetDoc) {
            targetDoc = this.ScUiMan().TopLevelDoc();
        }
        let bodyTag = targetDoc.ContentDoc.getElementsByTagName('body')[0];
        var flagElem = targetDoc.ContentDoc.createElement('div');
        flagElem.innerHTML = '<div>' + Message + '</div>';
        flagElem.style.position = 'absolute';
        flagElem.style.top = '100px';
        flagElem.style.left = '100px';
        flagElem.style.backgroundColor = 'yellow';
        flagElem.style.zIndex = '999';
        flagElem.style.fontSize = '40px';
        setTimeout(function () {
            flagElem.remove();
            window.close();
        }, InjectConst_ContentConst.Const.Timeouts.WaitBeforeRemovingCompleteFlagOnContent);
        bodyTag.appendChild(flagElem);
    }
    ReqMsgRouter(payload) {
        return ContentMessageManager_awaiter(this, void 0, void 0, function* () {
            return new Promise((resolve, reject) => ContentMessageManager_awaiter(this, void 0, void 0, function* () {
                this.AllAgents.Logger.FuncStart(this.ReqMsgRouter.name, StaticHelpers_StaticHelpers.MsgFlagAsString(payload.MsgFlag));
                let promResult = new PromiseResult(this.ReqMsgRouter.name, this.AllAgents.Logger);
                var response = yield this.ContentFactory().NewMsgFromContentShell();
                switch (payload.MsgFlag) {
                    case MsgFlag.ReqRestoreToNewTab:
                        console.log('we are going to restore to this window');
                        break;
                    case MsgFlag.ReqAddCETab:
                        let targetDoc = this.ScUiMan().TopLevelDoc();
                        let allIframeDataAtBeginning;
                        yield this.AllAgents.HelperAgent.PromisesRecipes.FromDesktopOpenNewCEIframe(targetDoc)
                            .then(() => promResult.MarkSuccessful())
                            .catch((err) => promResult.MarkFailed(err));
                        break;
                    case MsgFlag.ReqAdminB:
                        this.ScUiMan().AdminB(this.ScUiMan().TopLevelDoc(), null);
                        break;
                    case MsgFlag.Ping:
                        this.AllAgents.Logger.LogVal('Ping', StaticHelpers_StaticHelpers.MsgFlagAsString(payload.MsgFlag));
                        if (this.ReadyForMessages) {
                            response.MsgFlag = MsgFlag.RespListeningAndReady;
                            promResult.MarkSuccessful();
                        }
                        else {
                            promResult.MarkFailed('not ready');
                        }
                        break;
                    case MsgFlag.ReqCurState:
                        response.MsgFlag = MsgFlag.RespCurState;
                        promResult.MarkSuccessful();
                        break;
                    case MsgFlag.ReqOpenCE:
                        break;
                    case MsgFlag.ReqMarkFavorite:
                        this.AtticMan().MarkFavorite(payload.Data)
                            .then(() => promResult.MarkSuccessful())
                            .catch((err) => promResult.MarkFailed(err));
                        break;
                    case MsgFlag.ReqQuickPublish:
                        yield this.OneScWinMan().PublishActiveCE(this.ScUiMan().TopLevelDoc())
                            .then(() => promResult.MarkSuccessful())
                            .catch((err) => promResult.MarkFailed(err));
                        break;
                    case MsgFlag.ReqRestoreClick:
                        yield this.__restoreClick(payload.Data)
                            .then(() => promResult.MarkSuccessful())
                            .catch((err) => promResult.MarkFailed(err));
                        break;
                    case MsgFlag.ReqCompactCE:
                        yield this.SetCompactCss(payload.Data)
                            .then(() => promResult.MarkSuccessful())
                            .catch((err) => promResult.MarkFailed(err));
                        break;
                    case MsgFlag.ReqTakeSnapShot:
                        yield this.OneScWinMan().SaveWindowState(payload.Data.SnapShotSettings)
                            .then(() => { this.AllAgents.Logger.MarkerA(); })
                            .then(() => { this.AllAgents.Logger.MarkerA(); })
                            .then(() => promResult.MarkSuccessful())
                            .catch((err) => promResult.MarkFailed(err));
                        promResult.MarkSuccessful();
                        break;
                    case MsgFlag.RemoveFromStorage:
                        yield this.AtticMan().RemoveOneFromStorage(payload.Data.IdOfSelect)
                            .then(() => promResult.MarkSuccessful())
                            .catch((err) => promResult.MarkFailed(err));
                        break;
                    case MsgFlag.RespTaskSuccessful:
                        this.NotifyCompleteOnContent(null, payload.Data.ScreenMessage);
                    case MsgFlag.ReqUpdateNickName:
                        yield this.AtticMan().UpdateNickname(payload.Data)
                            .then(() => promResult.MarkSuccessful())
                            .catch((err) => promResult.MarkFailed(err));
                        break;
                    default:
                        this.AllAgents.Logger.ErrorAndThrow('Unhandled MsgFlag', StaticHelpers_StaticHelpers.MsgFlagAsString(payload.MsgFlag));
                        promResult.MarkFailed('Unhandled MsgFlag ' + StaticHelpers_StaticHelpers.MsgFlagAsString(payload.MsgFlag));
                        break;
                }
                yield this.ContentFactory().UpdateContentState(response.ContentState)
                    .then((result) => response.ContentState = result)
                    .catch((err) => promResult.MarkFailed(err));
                response.ContentState.LastReq = payload.MsgFlag;
                if (promResult.WasSuccessful()) {
                    response.MsgFlag = MsgFlag.RespTaskSuccessful;
                    resolve(response);
                }
                else {
                    reject(promResult.RejectReasons);
                }
                this.AllAgents.Logger.FuncEnd(this.ReqMsgRouter.name);
            }));
        });
    }
    SetCompactCss(Data) {
        return new Promise((resolve, reject) => ContentMessageManager_awaiter(this, void 0, void 0, function* () {
            this.AllAgents.Logger.FuncStart(this.SetCompactCss.name);
            var targetDoc = this.ScUiMan().TopLevelDoc();
            if (targetDoc) {
                var self = this;
                yield this.OneScWinMan().SetCompactCss(targetDoc)
                    .then(() => resolve())
                    .catch((err) => { this.AllAgents.Logger.ErrorAndThrow(this.SetCompactCss.name, err); });
            }
            this.AllAgents.Logger.FuncEnd(this.SetCompactCss.name);
        }));
    }
    __restoreClick(Data) {
        return new Promise((resolve, reject) => ContentMessageManager_awaiter(this, void 0, void 0, function* () {
            this.AllAgents.Logger.FuncStart(this.__restoreClick.name);
            let promiseResult = new PromiseResult(this.__restoreClick.name, this.AllAgents.Logger);
            try {
                this.AllAgents.Logger.MarkerA();
                if (Data.IdOfSelect) {
                    this.AllAgents.Logger.LogVal("IdOfSelect", Data.IdOfSelect);
                    var dataOneWindowStorage;
                    yield this.AtticMan().GetFromStorageById(Data.IdOfSelect, CacheMode.OkToUseCache)
                        .then((result) => dataOneWindowStorage = result);
                    var self = this;
                    var targetDoc = this.ScUiMan().TopLevelDoc();
                    if (targetDoc) {
                        yield self.OneScWinMan().RestoreWindowStateToTargetDoc(targetDoc, dataOneWindowStorage)
                            .then(() => promiseResult.MarkSuccessful())
                            .catch((err) => promiseResult.MarkFailed(err));
                    }
                    else {
                        self.AllAgents.Logger.ErrorAndThrow(this.__restoreClick.name, 'no target window');
                    }
                }
                else {
                    promiseResult.MarkFailed('No IdOfSelect');
                }
            }
            catch (ex) {
                this.AllAgents.Logger.ErrorAndThrow(this.__restoreClick.name, ex);
            }
            this.AllAgents.Logger.FuncEnd(this.__restoreClick.name);
            if (promiseResult.WasSuccessful()) {
                resolve();
            }
            else {
                reject(promiseResult.RejectReasons);
            }
        }));
    }
    IsLogEnabled() {
        return this.AllAgents.Logger.EnabledStatus();
    }
    SetParentInfo(__winDataParent) {
    }
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Content/scripts/Classes/ContentFactories.js
var ContentFactories_awaiter = (undefined && undefined.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};








class ContentFactories_ContentFactories extends ContentManagerBase {
    constructor(hub, contentAgents) {
        super(hub, contentAgents);
        this.AllAgents.Logger.FuncStart(PromiseGeneric_PromisesBasic.name);
        this.AllAgents.Logger.FuncEnd(PromiseGeneric_PromisesBasic.name);
    }
    UpdateContentState(contentState) {
        return new Promise((resolve, reject) => ContentFactories_awaiter(this, void 0, void 0, function* () {
            this.AllAgents.Logger.FuncStart(this.UpdateContentState.name);
            let promiseResult = new PromiseResult(this.UpdateContentState.name, this.AllAgents.Logger);
            contentState.SnapShotsMany = yield this.AtticMan().GetAllSnapShotsMany(CacheMode.OkToUseCache);
            contentState.ErrorStack = this.AllAgents.Logger.ErrorStack;
            yield this.GetCurrentDtOrCeState()
                .then((result) => { contentState.ActiveCe = result; })
                .then(() => promiseResult.MarkSuccessful())
                .catch((err) => promiseResult.MarkFailed(err));
            this.AllAgents.Logger.FuncEnd(this.UpdateContentState.name);
            if (promiseResult.WasSuccessful()) {
                resolve(contentState);
            }
            else {
                reject(promiseResult.RejectReasons);
            }
        }));
    }
    GetCurrentDtOrCeState() {
        return new Promise((resolve, reject) => ContentFactories_awaiter(this, void 0, void 0, function* () {
            this.AllAgents.Logger.FuncStart(this.GetCurrentDtOrCeState.name);
            let promiseResult = new PromiseResult(this.GetCurrentDtOrCeState.name, this.AllAgents.Logger);
            let toReturnOneTreeState = null;
            let pageType = this.ScUiMan().GetCurrentPageType();
            if (pageType === scWindowType.Desktop) {
                var currState;
                yield this.OneScWinMan().OneDesktopMan.GetStateDesktop()
                    .then((result) => {
                    toReturnOneTreeState = result.ActiveCeState;
                    promiseResult.MarkSuccessful();
                })
                    .catch((ex) => promiseResult.MarkFailed(ex));
            }
            else if (pageType === scWindowType.ContentEditor) {
                toReturnOneTreeState = null;
                yield this.OneScWinMan().OneCEAgent.GetTreeState(this.AllAgents.HelperAgent.GuidHelper.NewGuid())
                    .then((result) => {
                    toReturnOneTreeState = result;
                    promiseResult.MarkSuccessful();
                })
                    .catch((ex) => promiseResult.MarkFailed(ex));
            }
            else if (pageType === scWindowType.LoginPage
                || pageType === scWindowType.Launchpad
                || pageType === scWindowType.Edit
                || pageType === scWindowType.Preview
                || pageType === scWindowType.Normal) {
                toReturnOneTreeState = null;
                promiseResult.MarkSuccessful();
            }
            else {
                toReturnOneTreeState = null;
                promiseResult.MarkFailed('not a known page type ' + StaticHelpers_StaticHelpers.WindowTypeAsString(pageType));
            }
            this.AllAgents.Logger.FuncEnd(this.GetCurrentDtOrCeState.name);
            if (promiseResult.WasSuccessful()) {
                resolve(toReturnOneTreeState);
            }
            else {
                reject(promiseResult.RejectReasons);
            }
        }));
    }
    NewMsgFromContentShell() {
        return ContentFactories_awaiter(this, void 0, void 0, function* () {
            this.AllAgents.Logger.FuncStart(this.NewMsgFromContentShell.name);
            var response = new MsgPayloadResponseFromContent_MsgFromContent(MsgFlag.Unknown);
            this.AllAgents.Logger.FuncEnd(this.NewMsgFromContentShell.name);
            return response;
        });
    }
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Content/scripts/Managers/SitecoreUiManager.js


class SitecoreUiManager_SitecoreUiManager extends ContentManagerBase {
    GetCurrentPageType() {
        return this.AllAgents.HelperAgent.UrlHelp.CalcPageTypeFromHref({ AbsUrl: document.location.href });
    }
    TopLevelDoc() {
        if (!this.topDoc) {
            this.topDoc = {
                ContentDoc: window.document,
                DocId: this.AllAgents.HelperAgent.GuidHelper.NewGuid(),
                Nickname: 'top doc'
            };
        }
        return this.topDoc;
    }
    AdminB(targetDoc, callbackOnComplete) {
        this.AllAgents.Logger.FuncStart(this.AdminB.name, 'targetDoc: ' + targetDoc.DocId.AsShort);
        this.AllAgents.Logger.Log('callback passed: ' + (callbackOnComplete !== null));
        var userNameElem = targetDoc.ContentDoc.getElementById(InjectConst_ContentConst.Const.ElemId.sc.scLoginUserName);
        var passwordElem = targetDoc.ContentDoc.getElementById(InjectConst_ContentConst.Const.ElemId.sc.scLoginPassword);
        if (this.AllAgents.Logger.IsNotNullOrUndefinedBool('userNameElem', userNameElem)
            &&
                this.AllAgents.Logger.IsNotNullOrUndefinedBool('passwordElem', passwordElem)) {
            userNameElem.setAttribute('value', InjectConst_ContentConst.Const.Names.scDefaultAdminUserName);
            passwordElem.setAttribute('value', InjectConst_ContentConst.Const.Names.scDefaultAdminPassword);
            var loginButton = this.GetLoginButton(targetDoc);
            if (this.AllAgents.Logger.IsNotNullOrUndefinedBool('loginButton', loginButton)) {
                this.AllAgents.Logger.Log('clicking');
                loginButton.click();
                if (callbackOnComplete) {
                    this.AllAgents.Logger.Log('Triggering callback');
                    setTimeout(callbackOnComplete, InjectConst_ContentConst.Const.Timeouts.PostLoginBtnClick);
                }
                else {
                    this.AllAgents.Logger.Log('no callback passed');
                }
            }
            else {
                this.AllAgents.Logger.ErrorAndThrow(this.AdminB.name, 'No loginButton');
            }
        }
        else {
            this.AllAgents.Logger.ErrorAndThrow(this.AdminB.name, 'No Username or password field');
        }
        this.AllAgents.Logger.FuncEnd(this.AdminB.name);
    }
    GetLoginButton(targetDoc) {
        this.AllAgents.Logger.FuncStart(this.GetLoginButton.name);
        var toReturn = targetDoc.ContentDoc.getElementById(InjectConst_ContentConst.Const.ElemId.sc.scLoginBtn.sc920);
        if (!toReturn) {
            toReturn = targetDoc.ContentDoc.querySelector(InjectConst_ContentConst.Const.Selector.SC.LoginBtn.sc820);
        }
        this.AllAgents.Logger.Log('toReturn: ' + toReturn);
        this.AllAgents.Logger.FuncEnd(this.GetLoginButton.name);
        return toReturn;
    }
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Content/scripts/Managers/ContentHub.js









class ContentHub_ContentHub {
    constructor(allAgents) {
        this.ReadyForMessages = false;
        this.AllAgents = allAgents;
        this.AllAgents.Logger.IsNotNullOrUndefinedBool("AllAgents.HelperAgent", this.AllAgents.HelperAgent);
        this.AllAgents.Logger.FuncStart(ContentHub_ContentHub.name);
        console.log('(ctor) logger enabled ' + this.AllAgents.Logger.EnabledStatus());
        this.Instantiate();
        this.AllAgents.Logger.FuncEnd(ContentHub_ContentHub.name);
    }
    Instantiate() {
        this.AllAgents.Logger.FuncStart(this.Instantiate.name);
        this.AtticMan = new ContentAtticManager_ContentAtticManager(this, this.AllAgents);
        this.MsgMan = new ContentMessageManager_ContentMessageManager(this, this.AllAgents);
        this.MiscMan = new MiscManager_MiscManager(this, this.AllAgents);
        this.ContentFactory = new ContentFactories_ContentFactories(this, this.AllAgents);
        this.AllAgents.Logger.MarkerA();
        this.OneWindowMan = new OneScWindowManager_OneScWindowManager(this, this.AllAgents);
        this.PromiseOneStep = new PromiseOneStep_PromiseOneStep(this, this.AllAgents);
        this.SharedConst = SharedConst.Const;
        this.SitecoreUiMan = new SitecoreUiManager_SitecoreUiManager(this, this.AllAgents);
        this.Init();
        this.AllAgents.Logger.Log('ready for messages');
        this.ReadyForMessages = true;
        this.AllAgents.Logger.FuncEnd(this.Instantiate.name);
    }
    Init() {
        this.AllAgents.Logger.FuncStart(ContentHub_ContentHub.constructor.name + ' ' + this.Init.name);
        this.Const = InjectConst_ContentConst.Const;
        this.AtticMan.Init();
        this.MsgMan.Init();
        this.AllAgents.Logger.SetEnabled(this.MsgMan.IsLogEnabled());
        this.OneWindowMan.Init();
        this.AllAgents.Logger.FuncEnd(ContentHub_ContentHub.constructor.name + ' ' + this.Init.name);
    }
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Shared/scripts/Enums/SettingFlavor.js
var SettingFlavor;
(function (SettingFlavor) {
    SettingFlavor[SettingFlavor["unknown"] = 0] = "unknown";
    SettingFlavor[SettingFlavor["PopUp"] = 1] = "PopUp";
    SettingFlavor[SettingFlavor["ContentAndPopUpStoredInPopUp"] = 2] = "ContentAndPopUpStoredInPopUp";
    SettingFlavor[SettingFlavor["ContentAndPopUpStoredInEach"] = 3] = "ContentAndPopUpStoredInEach";
})(SettingFlavor || (SettingFlavor = {}));

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/PopUp/scripts/Classes/PopConst.js

let PopConst_PopConst = (() => {
    class PopConst {
    }
    PopConst.Const = {
        ClassNames: {
            HS: {
                Collapsed: 'in',
            },
        },
        ElemId: {
            textAreaFeedback: 'ta-feedback',
            InputNickname: 'inputNickname',
            HindSiteParentInfo: 'spanParentInfo',
            HS: {
                SelectHeaderAuto: 'sel-header-auto',
                SelectHeaderAutoTitle: 'sel-header-auto-title',
                SelectHeaderFavorite: 'sel-header-favorite',
                SelectHeaderFavoriteTitle: 'sel-header-favorite-title',
                TabId: 'orig-win-id',
                Btn: {
                    AdminB: 'AdminB',
                    MarkFavorite: 'MarkFavorite',
                    HsCancel: 'Cancel',
                    GoCE: 'CE',
                    Desktop: 'Desktop',
                    HsDrawStorage: 'DrawLocalStorage',
                    DrawPopUpLogStorage: 'DrawPopUpLogStorage',
                    BigRed: 'BigRed',
                    ModeEdit: 'sc-mode-edit',
                    ModeNorm: 'Norm',
                    ModePrev: 'Prev',
                    PresentationDetails: 'PresentationDetails',
                    CompactCE: 'CompactCE',
                    QuickPublish: 'QuickPublish',
                    HsRemoveFromStorage: 'RemoveOneFromLocalStorage',
                    HsRestoreWindowState: 'RestoreWindowState',
                    TakeSnapshot: 'SaveWindowState',
                    UpdateNicknameB: 'UpdateNickname',
                },
            }
        },
        Numbers: {
            MinMenuHeight: 600,
            MinMenuWidth: 600,
            MaxAutoSaveCount: 10,
            AutoSaveIntervalMin: 5,
        },
        Notify: {
            Default: 'Complete',
            PublishComplete: 'Publishing Complete',
        },
        ScMode: {
            edit: { AsEnum: scMode_scMode.Edit, AsString: 'edit' },
            normal: { AsEnum: scMode_scMode.Normal, AsString: 'normal' },
            preview: { AsEnum: scMode_scMode.Preview, AsString: 'preview' },
        },
        Selector: {
            Btn: {
                WindowClose: '[id=close-window]',
            },
            Legend: {
                LgndSettings: '[id=lgnd-settings]',
                LgndInSite: '[id=lgnd-in-site]',
                LgndHindSite: '[id=lgnd-hind-site]',
                LgndDebug: '[id=lgnd-log]',
                LgndForeSite: '[id=lgnd-fore-site]',
                LgndState: '[id=lgnd-state]',
            },
            HS: {
                GenericSettingTest: '[id=id-generic-setting-test]',
                LogToConsole: '[id=id-settings-show-log-data]',
                iCBoxdSettingsAutoLogin: '[id=id-settings-auto-login]',
                PrefAutoLogin: '[id=id-settings-auto-login]',
                SettingNotUsed: '[id=id-settings-not-used]',
                AutoSnapshotBeforeWindowChange: '[id=id-settings-auto-snapshot-on-window-change]',
                SettingAutoSaveInterval: '[id=id-settings-auto-save-interval]',
                SettingAutoSaveMaxCount: '[id=id-settings-auto-save-max-count]',
                SettingDebugKeepDialogOpen: '[id=id-settings-debug-keep-dialog-open]',
                BuildStamp: '[id=build-stamp]',
                DivMsgStatus: '[id=msg-status]',
                DivStatePopUp: '[id=div-state-pop-up]',
                DivStateContent: '[id=div-state-content]',
                IdFieldSetDebug: '[id=id-fieldset-debug]',
                SelStateSnapShot: '[id=selState]',
                TaDebug: '[id=ta-log]',
            },
        },
        Settings: {
            Defaults: {
                LgndDebug: false,
                LgndForeSite: true,
                LgndHindSite: true,
                LgndInSite: true,
                LgndSettings: false,
                LgndState: false,
                DebugKeepDialogOpen: false,
                AutoSaveEnabled: false,
            }
        },
        Storage: {
            SettingsSuffix: 'todo',
            KeyGenericSettings: 'HindSite.' + 'GenericSettings',
            StorageLogKeyPrefix: 'HindSite.' + 'Log.',
            Defaults: {
                bool: {
                    AutoSaveEnabled: false,
                    AutoLogin: false,
                    DefaultDebugKeepDialogOpen: false,
                    AutoSnapshotBeforeWindowChange: true,
                }
            }
        },
        Timeouts: {
            WaitBeforeRemovingCompleteFlag: 1500,
            AutoLoginCheckInterval: 3000,
        },
    };
    return PopConst;
})();


// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Shared/scripts/Agents/Agents/SettingsAgent/SettingsAgent.js
var SettingsAgent_awaiter = (undefined && undefined.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};



class SettingsAgent_SettingsAgent {
    constructor(logger, repoAgent) {
        this.SettingsAr = [];
        this.Logger = logger;
        this.RepoAgent = repoAgent;
    }
    SetContentSettings(currentContentPrefs) {
        this.SettingsAr = currentContentPrefs;
    }
    InitSettingsAgent(allDefaultSettings) {
        return SettingsAgent_awaiter(this, void 0, void 0, function* () {
            return new Promise((resolve) => SettingsAgent_awaiter(this, void 0, void 0, function* () {
                this.Logger.FuncStart(this.InitSettingsAgent.name, allDefaultSettings.length);
                this.SettingsAr = allDefaultSettings;
                yield this.HarvestGenericSettingsFromStorage()
                    .then(() => resolve());
                this.Logger.FuncEnd(this.InitSettingsAgent.name);
            }));
        });
    }
    ReadGenericSettings() {
        return SettingsAgent_awaiter(this, void 0, void 0, function* () {
            return new Promise((resolve) => SettingsAgent_awaiter(this, void 0, void 0, function* () {
                this.Logger.FuncStart(this.ReadGenericSettings.name);
                let toReturn = [];
                yield this.RepoAgent.ReadDataOfKey(PopConst_PopConst.Const.Storage.KeyGenericSettings)
                    .then((storedValue) => {
                    if (storedValue) {
                        toReturn = JSON.parse(storedValue.toString());
                    }
                    else {
                        toReturn = null;
                    }
                })
                    .then(() => resolve(toReturn));
                this.Logger.FuncEnd(this.ReadGenericSettings.name);
            }));
        });
    }
    UpdateSettingValuesFromStorage(foundSettings) {
        this.Logger.FuncStart(this.UpdateSettingValuesFromStorage.name);
        for (var idx = 0; idx < foundSettings.length; idx++) {
            let storageSetting = foundSettings[idx];
            this.Logger.LogVal('setting key', storageSetting.SettingKeyFriendly);
            let matchingSetting = this.GetByKey(storageSetting.SettingKey);
            if (matchingSetting) {
                matchingSetting.ValueAsObj = storageSetting.ValueAsObj;
            }
            else {
                this.Logger.ErrorAndThrow(this.UpdateSettingValuesFromStorage.name, 'matching setting not found ' + StaticHelpers_StaticHelpers.SettingKeyAsString(storageSetting.SettingKey));
            }
        }
        this.Logger.FuncEnd(this.UpdateSettingValuesFromStorage.name);
    }
    HarvestGenericSettingsFromStorage() {
        return SettingsAgent_awaiter(this, void 0, void 0, function* () {
            return new Promise((resolve) => SettingsAgent_awaiter(this, void 0, void 0, function* () {
                this.Logger.FuncStart(this.HarvestGenericSettingsFromStorage.name);
                let foundSettings;
                yield this.ReadGenericSettings()
                    .then((result) => foundSettings = result)
                    .then(() => this.Logger.LogAsJsonPretty('settings from storage', foundSettings))
                    .then(() => {
                    if (foundSettings) {
                        this.UpdateSettingValuesFromStorage(foundSettings);
                    }
                })
                    .then(() => resolve());
                this.Logger.FuncEnd(this.HarvestGenericSettingsFromStorage.name);
            }));
        });
    }
    ValueAsInteger(setting) {
        let toReturn = 0;
        if (setting) {
            toReturn = setting.ValueAsObj;
        }
        return toReturn;
    }
    GetOnlyContentPrefs() {
        let toReturn = [];
        for (var idx = 0; idx < this.SettingsAr.length; idx++) {
            let candidate = this.SettingsAr[idx];
            if (candidate.SettingFlavor === SettingFlavor.ContentAndPopUpStoredInPopUp) {
                toReturn.push(candidate);
            }
        }
        return toReturn;
    }
    SettingChanged(SettingKey, valueAsObj) {
        this.Logger.Log(StaticHelpers_StaticHelpers.SettingKeyAsString(SettingKey));
        this.Logger.LogVal('valueAsObj', valueAsObj.toString());
        this.SetByKey(SettingKey, valueAsObj);
    }
    GetByKey(settingKey) {
        this.Logger.FuncStart(this.GetByKey.name, StaticHelpers_StaticHelpers.SettingKeyAsString(settingKey));
        var toReturn;
        for (var idx = 0; idx < this.SettingsAr.length; idx++) {
            if (this.SettingsAr[idx].SettingKey === settingKey) {
                this.Logger.Log('Setting found');
                toReturn = this.SettingsAr[idx];
                this.Logger.LogAsJsonPretty('ValueAsObj', toReturn.ValueAsObj);
                break;
            }
        }
        this.Logger.FuncEnd(this.GetByKey.name);
        return toReturn;
    }
    SetByKey(settingKey, value) {
        this.Logger.FuncStart(this.SetByKey.name, StaticHelpers_StaticHelpers.SettingKeyAsString(settingKey));
        this.Logger.LogAsJsonPretty('value', value);
        let foundSetting = this.GetByKey(settingKey);
        if (foundSetting) {
            if (foundSetting.DefaultValue !== value) {
                foundSetting.ValueAsObj = value;
            }
            else {
                foundSetting.ValueAsObj = null;
            }
            let settingValues = [];
            for (var udx = 0; udx < this.SettingsAr.length; udx++) {
                if (this.SettingsAr[udx].ValueAsObj !== null) {
                    settingValues.push({
                        SettingKey: this.SettingsAr[udx].SettingKey,
                        ValueAsObj: this.SettingsAr[udx].ValueAsObj,
                        SettingKeyFriendly: StaticHelpers_StaticHelpers.SettingKeyAsString(this.SettingsAr[udx].SettingKey)
                    });
                }
            }
            this.RepoAgent.WriteGenericSettings(settingValues);
        }
        else {
            this.Logger.ErrorAndThrow(this.SetByKey.name, 'setting match not found');
        }
        this.Logger.FuncEnd(this.SetByKey.name, StaticHelpers_StaticHelpers.SettingKeyAsString(settingKey));
    }
    ValueAsBool(setting) {
        let toReturn = false;
        if (setting) {
            toReturn = setting.ValueAsObj;
        }
        return toReturn;
    }
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Shared/scripts/Agents/Agents/LoggerAgent/LoggerAgent.js
var LoggerAgent_awaiter = (undefined && undefined.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};





class LoggerAgent_LoggerAgent {
    constructor() {
        this.LogHasBeenInit = false;
        this.ErrorStack = [];
        this.LogPreInitBuffer = [];
        this.__debugTextChangedCallbacks = [];
        this.CurrentStorageLogKey = 'default';
        this.LogTabId = -1;
        this.LogTab = null;
        this.debugPrefix = '\t\t';
        this.StorageLogCombined = "";
        this.__callDepth = -1;
        console.log('default: ' + SharedConst.Const.Settings.Defaults.LogToConsole);
        this.LogToConsoleEnabled = SharedConst.Const.Settings.Defaults.LogToConsole;
        this.LogToStorageEnabled = SharedConst.Const.Settings.Defaults.LogToStorage;
        this.LogHasBeenInit = false;
        console.log('(ctor) Logger log to console enabled: ' + this.LogToConsoleEnabled);
        console.log('(ctor) Logger log to storage enabled: ' + this.LogToStorageEnabled);
    }
    SetLogToStorageKey(currentStorageLogKey) {
        console.log(this.SetLogToStorageKey.name + ' ' + currentStorageLogKey);
        this.CurrentStorageLogKey = currentStorageLogKey;
    }
    Init(val) {
        return LoggerAgent_awaiter(this, void 0, void 0, function* () {
            this.LogToConsoleEnabled = val;
            this.LogHasBeenInit = true;
            if (this.LogToConsoleEnabled) {
                var iterCheckMax = 1000;
                while (this.LogPreInitBuffer.length > 0 && iterCheckMax > 0) {
                    iterCheckMax--;
                    this.Log(this.LogPreInitBuffer.shift());
                }
            }
            console.log('(init) Logger log to console enabled: ' + this.LogToConsoleEnabled);
            console.log('(init) Logger log to storage enabled: ' + this.LogToStorageEnabled);
        });
    }
    SetEnabled(newValue) {
        this.LogToConsoleEnabled = newValue;
        console.log('Logging set to: ' + newValue);
    }
    DebugIDataBrowserTab(browserWindow) {
        if (this.IsNotNullOrUndefinedBool('IDataBrowserWindow', browserWindow)) {
            this.LogVal('WindowType', scWindowType[browserWindow.UrlParts.ScWindowType]);
        }
    }
    EnabledStatus() {
        return this.LogToConsoleEnabled;
    }
    DebugIdataPopUpSettings(toReturn) {
        this.LogVal('Settings', JSON.stringify(toReturn));
    }
    DebugWindow(window) {
        if (this.IsNotNullOrUndefinedBool('window', window)) {
        }
    }
    ThrowIfNullOrUndefined(title, subject) {
        if (!this.IsNotNullOrUndefinedBool(title, subject)) {
            throw 'Failed';
        }
    }
    IsNotNullOrUndefinedBool(title, subject) {
        var toReturn = false;
        if (subject) {
            if ((typeof subject) === 'undefined') {
                this.LogVal(title + ' Is Not Undefined', '!!! false !!!');
            }
            else {
                this.LogVal(title + ' Is Not Null', 'true');
                toReturn = true;
            }
        }
        else {
            this.LogVal(title + ' Is Not Null', '!!! false !!!');
        }
        return toReturn;
    }
    DebugIGuid(id) {
        if (this.IsNotNullOrUndefinedBool('IGuid', id)) {
            this.LogVal('asShort', id.AsShort);
            this.LogVal('asString', id.AsString);
        }
    }
    DebugIDataOneDoc(dataOneDoc) {
        this.FuncStart(this.DebugIDataOneDoc.name);
        if (dataOneDoc) {
            if (dataOneDoc.ContentDoc) {
                this.LogVal(this.debugPrefix + 'dataOneDoc.Document.readyState:', dataOneDoc.ContentDoc.readyState);
                if (dataOneDoc.ContentDoc.location) {
                    this.LogVal(this.debugPrefix + 'targetDoc.location.href', dataOneDoc.ContentDoc.location.href);
                }
                else {
                    this.Log(this.debugPrefix + 'dataOneDoc.Document.location - does not exist');
                }
            }
            else {
                this.Log(this.debugPrefix + 'dataOneDoc.Document - does not exist');
            }
        }
        else {
            this.ErrorAndThrow(this.DebugIDataOneDoc.name, 'no targetDoc');
        }
        this.Log('');
        this.FuncEnd(this.DebugIDataOneDoc.name);
    }
    AddDebugTextChangedCallback(caller, callback) {
        this.__debugTextChangedCallbacks.push({
            Caller: caller,
            Func: callback
        });
    }
    HndlrClearDebugText(self, verify = false) {
        this.FuncStart(this.HndlrClearDebugText.name);
        var proceed = true;
        if (verify) {
            proceed = confirm('Clear Debug TextArea ?');
        }
        if (proceed) {
            var newText = '--- Debug Text Reset ---';
            self.__triggerAllDebugTextChangedCallbacks({
                NewText: newText,
                Append: false
            });
        }
        this.FuncEnd(this.HndlrClearDebugText.name);
    }
    MarkerA() {
        this.__markerRaw('A');
    }
    MarkerB() {
        this.__markerRaw('B');
    }
    MarkerC() {
        this.__markerRaw('C');
    }
    MarkerD() {
        this.__markerRaw('D');
    }
    MarkerE() { this.__markerRaw('E'); }
    MarkerF() { this.__markerRaw('F'); }
    __markerRaw(marker) {
        this.Log('Marker ' + marker);
    }
    LogAsJsonPretty(texValName, jsonObj) {
        this.LogVal(texValName, JSON.stringify(jsonObj, null, 1));
    }
    LogVal(textValName, textVal) {
        if (typeof textVal === 'undefined') {
            textVal = '{undefined}';
        }
        else if (textVal === null) {
            textVal = '{null}';
        }
        else {
            var asGuid = textVal;
            if (asGuid.Type === 'IGuid') {
                textVal = asGuid.AsString;
            }
        }
        textVal = textVal.toString();
        textValName = StaticHelpers_StaticHelpers.BufferString(textValName.toString(), 50, BufferChar.space, BufferDirection.right);
        const debugPrefix = '  ~~~  ';
        this.Log(debugPrefix + textValName + ' : ' + textVal);
    }
    Log(text, optionalValue = '', hasPrefix = false) {
        return LoggerAgent_awaiter(this, void 0, void 0, function* () {
            if (this.LogToConsoleEnabled || !this.LogHasBeenInit) {
                var indent = '  ';
                for (var idx = 0; idx < this.__callDepth; idx++) {
                    text = indent + text;
                }
                var prefixLength = 3;
                if (!hasPrefix) {
                    for (var idx = 0; idx < prefixLength; idx++) {
                        text = ' ' + text;
                    }
                }
                this.__triggerAllDebugTextChangedCallbacks({
                    NewText: text,
                    Append: true
                });
                if (this.LogToConsoleEnabled) {
                    console.log(text);
                    yield this.WriteLogToStorage(text);
                }
                else if (!this.LogHasBeenInit) {
                    this.LogPreInitBuffer.push(text);
                }
            }
        });
    }
    WriteLogToStorage(logMessage) {
        return LoggerAgent_awaiter(this, void 0, void 0, function* () {
            return new Promise((resolve) => LoggerAgent_awaiter(this, void 0, void 0, function* () {
                this.StorageLogCombined += "|||" + JSON.stringify(logMessage);
                if (this.CurrentStorageLogKey) {
                    window.localStorage.setItem(this.CurrentStorageLogKey, this.StorageLogCombined);
                }
            }));
        });
    }
    DebugDataOneIframe(dataOneIframe) {
        this.FuncStart(this.DebugDataOneIframe.name);
        this.Log('dataOneIframe : ' + this.IsNullOrUndefined(dataOneIframe));
        if (dataOneIframe) {
            this.Log('dataOneIframe.Nickname : ' + dataOneIframe.Nickname);
            this.Log('dataOneIframe.IframeElem: \t' + this.IsNullOrUndefined(dataOneIframe.IframeElem));
            if (dataOneIframe.IframeElem) {
                this.Log('dataOneIframe.id: \t' + this.IsNullOrUndefined(dataOneIframe.IframeElem.id));
            }
            this.Log('dataOneIframe.ContentDoc: \t' + this.IsNullOrUndefined(dataOneIframe.ContentDoc));
            this.DebugIDataOneDoc(dataOneIframe.ContentDoc);
        }
        this.FuncEnd(this.DebugDataOneIframe.name);
    }
    DebugPayloadDataFromPopUp(data) {
        if (this.IsNotNullOrUndefinedBool('PayloadDataFromPopUp', data)) {
            this.LogVal('idOfSelect', data.IdOfSelect);
            this.DebugIGuid(data.IdOfSelect);
            this.DebugIDataPayloadSnapShot(data.SnapShotSettings);
        }
    }
    DebugIDataPayloadSnapShot(snapShotSettings) {
        if (this.IsNotNullOrUndefinedBool('IDataPayloadSnapShot', snapShotSettings)) {
            this.LogVal('Flavor', StaticHelpers_StaticHelpers.FlavorAsString(snapShotSettings.Flavor));
            this.LogVal('Nickname', snapShotSettings.SnapShotNewNickname);
        }
    }
    DebugObjState(state) {
        if (this.IsNotNullOrUndefinedBool('State', state)) {
            if (this.IsNotNullOrUndefinedBool('CurrentSnapShots', state.SnapShotsMany.CurrentSnapShots)) {
                this.LogVal('Snapshot count', state.SnapShotsMany.CurrentSnapShots.length);
            }
        }
    }
    PromiseBucketDebug(promiseBucket, friendlyName) {
        this.FuncStart(this.PromiseBucketDebug.name, friendlyName);
        this.Log('promiseBucket : ' + this.IsNullOrUndefined(promiseBucket));
        if (promiseBucket && typeof (promiseBucket) !== 'undefined') {
            this.Log('promiseBucket.IFramesbefore: ' + this.IsNullOrUndefined(promiseBucket.IFramesbefore));
            this.Log('promiseBucket.oneTreeState: ' + this.IsNullOrUndefined(promiseBucket.oneTreeState));
        }
        this.FuncEnd(this.PromiseBucketDebug.name, friendlyName);
    }
    __triggerAllDebugTextChangedCallbacks(data) {
        for (var idx = 0; idx < this.__debugTextChangedCallbacks.length; idx++) {
            var oneCallback = this.__debugTextChangedCallbacks[idx];
            oneCallback.Func(oneCallback.Caller, data);
        }
    }
    CtorName(ctorName) {
        this.Log('Constructor: ' + ctorName);
    }
    FuncStart(textOrFunc, optionalValue) {
        textOrFunc = 's' + ' ' + this.__callDepth + ') ' + textOrFunc;
        if (!optionalValue) {
            optionalValue = '';
        }
        else {
            optionalValue = optionalValue.toString();
        }
        if (optionalValue.length > 0) {
            textOrFunc = textOrFunc + ' : ' + optionalValue;
        }
        this.Log(textOrFunc, '', true);
        this.__callDepth++;
        if (this.__callDepth > 10) {
            this.__callDepth = 10;
        }
    }
    FuncEnd(text, optionalValueInput) {
        this.__callDepth--;
        if (this.__callDepth < 0) {
            this.__callDepth = 0;
        }
        text = 'e' + ' ' + this.__callDepth + ') ' + text;
        if (!optionalValueInput) {
            optionalValueInput = '';
        }
        var optionalValue = optionalValueInput.toString();
        if (optionalValue.length > 0) {
            text = text + ' : ' + optionalValue;
        }
        this.Log(text, optionalValue, true);
    }
    ErrorAndThrow(container, text) {
        if (!container) {
            container = 'unknown';
        }
        if (!text) {
            text = 'unknown';
        }
        this.ErrorStack.push({
            ContainerFunc: container,
            ErrorString: text
        });
        this.Log('');
        this.Log('\t\t** ERROR ** ' + container);
        this.Log('');
        this.Log('\t\t  ' + text);
        this.Log('');
        this.Log('\t\t** ERROR ** ' + container);
        this.Log('');
        throw container + " " + text;
    }
    NotNullCheck(title, value) {
        if (typeof value === 'undefined') {
            this.LogVal(title, 'Is Undefined');
        }
        else if (!value) {
            this.LogVal(title, 'Is Null');
        }
        else {
            this.LogVal(title, 'Is Not Null');
        }
    }
    IsNullOrUndefined(subject) {
        var toReturn = '{unknown}';
        if (subject) {
            if ((typeof subject) === 'undefined') {
                toReturn = 'Is Undefined';
            }
            else {
                toReturn = 'Not Null';
            }
        }
        else {
            toReturn = 'Is Null';
        }
        return toReturn;
    }
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Shared/scripts/Agents/Agents/RepositoryAgent/RepoAgent.js
var RepoAgent_awaiter = (undefined && undefined.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};

class RepoAgent_RepoAgent {
    constructor(loggerAgent) {
        this.Logger = loggerAgent;
    }
    Init() {
        this.Logger.FuncStart(RepoAgent_RepoAgent.name, this.Init.name);
        this.Logger.FuncEnd(RepoAgent_RepoAgent.name, this.Init.name);
    }
    GetBulkLocalStorageByKeyPrefix(targetPrefix) {
        return new Promise((resolve) => {
            var toReturn = [];
            var storageLength = window.localStorage.length;
            for (var idx = 0; idx < storageLength; idx++) {
                console.log('Processing Index' + idx);
                var candidate = {
                    data: '',
                    key: '',
                };
                candidate.key = window.localStorage.key(idx);
                console.log('Candidate.key ' + candidate.key);
                if (candidate.key.startsWith(targetPrefix)) {
                    console.log('valid candidate' + true);
                    candidate.data = window.localStorage.getItem(candidate.key);
                    if (typeof candidate != 'undefined' && typeof candidate.data != 'undefined' && candidate != null && candidate.data != null) {
                        toReturn.push(candidate);
                    }
                }
            }
            resolve(toReturn);
        });
    }
    ReadDataOfKey(targetKey) {
        return RepoAgent_awaiter(this, void 0, void 0, function* () {
            let toReturn = null;
            console.log('target key ' + targetKey);
            yield browser.storage.local.get(targetKey)
                .then((storageResults) => {
                console.log('storageResults: ' + JSON.stringify(storageResults));
                var storageKeys = Object.keys(storageResults);
                for (let oneKey of storageKeys) {
                    if (oneKey === targetKey) {
                        let storedValue = storageResults[oneKey];
                        if (storedValue) {
                            toReturn = storedValue.toString();
                        }
                    }
                }
            });
            return toReturn;
        });
    }
    WriteGenericSettings(settingsToWrite) {
        this.Logger.FuncStart(this.WriteGenericSettings.name);
        this.Logger.LogAsJsonPretty('settings', settingsToWrite);
        this.Logger.LogAsJsonPretty('key', PopConst_PopConst.Const.Storage.KeyGenericSettings);
        window.localStorage.setItem(PopConst_PopConst.Const.Storage.KeyGenericSettings, JSON.stringify(settingsToWrite));
        this.Logger.FuncEnd(this.WriteGenericSettings.name);
    }
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Shared/scripts/Helpers/GuidHelper.js


class GuidHelper_GuidHelper extends HelperBase {
    constructor() {
        super(...arguments);
        this.ShortGuidLength = 4;
    }
    EmptyGuidJustNumbers() {
        return '00000000000000000000000000000000';
    }
    EmptyGuid() {
        return this.ParseGuid('00000000-0000-0000-0000-000000000000');
    }
    newGuidString() {
        var toReturn;
        var temp = 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx';
        toReturn = temp.replace(/[xy]/g, function (c) {
            var r = Math.random() * 16 | 0;
            var v = c == 'x' ? r : (r & 0x3 | 0x8);
            return v.toString(16);
        });
        return toReturn;
    }
    NewGuid() {
        var toReturn = this.ParseGuid(this.newGuidString());
        return toReturn;
    }
    ShortGuid(Id) {
        var toReturn = '{error}';
        if (Id && Id.AsString.length > this.ShortGuidLength) {
            toReturn = Id.AsString.substr(0, this.ShortGuidLength);
        }
        else {
            this.Logger.DebugIGuid(Id);
            this.Logger.LogVal('Length', Id.AsString.length);
            this.Logger.LogVal('ShortLength', this.ShortGuidLength);
        }
        return toReturn;
    }
    FormatJustNumbers(str) {
        return str.replace(SharedConst.Const.Regex.CleanGuid, '');
    }
    FormatAsBracedGuid(str) {
        var GUID;
        try {
            var parts = [];
            if (str.length !== 32) {
                this.Logger.ErrorAndThrow(this.FormatAsBracedGuid.name, 'Wrong count wanted: ' + 32 + " got: " + str.length);
            }
            parts.push(str.slice(0, 8));
            parts.push(str.slice(8, 12));
            parts.push(str.slice(12, 16));
            parts.push(str.slice(16, 20));
            parts.push(str.slice(20, 32));
            GUID = '{' + parts.join('-') + '}';
        }
        catch (ex) {
            this.Logger.ErrorAndThrow(this.FormatAsBracedGuid.name, ex);
        }
        return GUID;
    }
    ParseGuid(val) {
        let justNumbers = this.FormatJustNumbers(val);
        let guidFormat = this.FormatAsBracedGuid(justNumbers);
        let toReturn = {
            AsString: justNumbers,
            AsBracedGuid: guidFormat,
            AsShort: '',
            Type: 'IGuid'
        };
        toReturn.AsShort = this.ShortGuid(toReturn);
        return toReturn;
    }
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Shared/scripts/Helpers/UtilityHelper.js






class UtilityHelper_UtilityHelper extends HelperBase {
    constructor() {
        super(...arguments);
        this.lenTimestamp = 13;
        this.lenNickname = 16;
        this.lenPageType = 7;
        this.lenPrefix = 6;
        this.lenShortId = 4;
        this.colSep = ' - ';
        this.lenCeCount = 3;
        this.lenActiveNode = 16;
        this.lenFavorite = 3;
    }
    SelectHeaderStr(prefix) {
        let toReturn = StaticHelpers_StaticHelpers.BufferString('Time Stamp', this.lenTimestamp, BufferChar.Period, BufferDirection.right)
            + this.colSep + StaticHelpers_StaticHelpers.BufferString('Type', this.lenPageType, BufferChar.Period, BufferDirection.right)
            + this.colSep + StaticHelpers_StaticHelpers.BufferString('Nickname', this.lenNickname, BufferChar.Period, BufferDirection.right)
            + this.colSep + StaticHelpers_StaticHelpers.BufferString('Active Node.', this.lenActiveNode, BufferChar.Period, BufferDirection.right)
            + this.colSep + StaticHelpers_StaticHelpers.BufferString('Fav.', this.lenFavorite, BufferChar.Period, BufferDirection.right)
            + this.colSep + StaticHelpers_StaticHelpers.BufferString('Id', this.lenShortId, BufferChar.Period, BufferDirection.right)
            + this.colSep + StaticHelpers_StaticHelpers.BufferString('#CE', this.lenCeCount, BufferChar.Period, BufferDirection.right);
        return toReturn;
    }
    MakeSelectorFromId(TabId) {
        return '[id=' + TabId + ']';
    }
    TimeNicknameFavStr(data) {
        var typeStr = '';
        if (data.WindowType === scWindowType.ContentEditor) {
            typeStr = 'Cont Ed';
        }
        else if (data.WindowType === scWindowType.Desktop) {
            typeStr = 'Desktop';
        }
        var activeCeNode = '';
        for (var idx = 0; idx < data.AllCEAr.length; idx++) {
            var candidateCe = data.AllCEAr[idx];
            for (var jdx = 0; jdx < candidateCe.AllTreeNodeAr.length; jdx++) {
                var candidateNode = candidateCe.AllTreeNodeAr[jdx];
                if (candidateNode.IsActive) {
                    var lvl2Node = '';
                    if (jdx >= 2) {
                        lvl2Node = candidateCe.AllTreeNodeAr[1].NodeFriendly + '/';
                    }
                    activeCeNode = lvl2Node + candidateNode.NodeFriendly;
                    break;
                }
            }
        }
        let toReturn = StaticHelpers_StaticHelpers.BufferString(this.MakeFriendlyDate(data.TimeStamp), this.lenTimestamp, BufferChar.space, BufferDirection.right)
            + this.colSep + StaticHelpers_StaticHelpers.BufferString(typeStr, this.lenPageType, BufferChar.Nbsp, BufferDirection.right)
            + this.colSep + StaticHelpers_StaticHelpers.BufferString(data.NickName, this.lenNickname, BufferChar.Nbsp, BufferDirection.right)
            + this.colSep + StaticHelpers_StaticHelpers.BufferString(activeCeNode, this.lenActiveNode, BufferChar.Nbsp, BufferDirection.right)
            + this.colSep + StaticHelpers_StaticHelpers.BufferString((data.Flavor === SnapShotFlavor.Favorite ? '*' : ''), this.lenFavorite, BufferChar.Nbsp, BufferDirection.right)
            + this.colSep + StaticHelpers_StaticHelpers.BufferString(data.Id.AsShort, this.lenShortId, BufferChar.Nbsp, BufferDirection.right)
            + this.colSep + StaticHelpers_StaticHelpers.BufferString(data.AllCEAr.length.toString(), this.lenCeCount, BufferChar.Nbsp, BufferDirection.right);
        return toReturn;
    }
    TimeNicknameFavStrForConfirmation(data) {
        var result = this.TimeNicknameFavStr(data);
        result = result.replace(new RegExp(/&nbsp;/ig), '');
        return result;
    }
    MakeFriendlyDate(date) {
        var toReturn = '';
        var workingDate = new Date(date);
        if (workingDate) {
            var month = StaticHelpers_StaticHelpers.BufferString((workingDate.getMonth() + 1).toString(), 2, BufferChar.Zero, BufferDirection.left);
            var day = StaticHelpers_StaticHelpers.BufferString(workingDate.getDate().toString(), 2, BufferChar.Zero, BufferDirection.left);
            var min = StaticHelpers_StaticHelpers.BufferString(workingDate.getMinutes().toString(), 2, BufferChar.Zero, BufferDirection.left);
            var hoursRaw = workingDate.getHours();
            var ampm = hoursRaw >= 12 ? 'p' : 'a';
            hoursRaw = hoursRaw % 12;
            var hourClean = hoursRaw ? hoursRaw : 12;
            var hourCleanStr = StaticHelpers_StaticHelpers.BufferString(hourClean.toString(), 2, BufferChar.Zero, BufferDirection.left);
            toReturn = month + '.' + day + ' ' + hourCleanStr + ':' + min + ' ' + ampm;
        }
        else {
            toReturn = '{error}';
            this.Logger.ErrorAndThrow(this.MakeFriendlyDate.name, 'no date provided');
        }
        return toReturn;
    }
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Shared/scripts/Helpers/FactoryHelper.js

class FactoryHelper_FactoryHelper extends HelperBase {
    CreateNewDtDataShell() {
        var toReturn = {
            AllCeData: [],
            livingIframeAr: [],
            ActiveCeMan: null,
            ActiveCeState: null
        };
        return toReturn;
    }
    DataOneContentDocFactoryFromIframe(dataOneIframe) {
        var toReturn = null;
        if (dataOneIframe) {
            toReturn =
                {
                    ContentDoc: dataOneIframe.IframeElem.contentDocument,
                    DocId: this.HelperAgent.GuidHelper.NewGuid(),
                    Nickname: dataOneIframe.Nickname + ' - content doc'
                };
        }
        else {
            this.Logger.ErrorAndThrow(this.DataOneContentDocFactoryFromIframe.name, 'no iframe provided');
        }
        return toReturn;
    }
    DataOneIframeFactory(iframeElem, nickname) {
        this.Logger.FuncStart(this.DataOneIframeFactory.name);
        var toReturn = null;
        if (iframeElem && nickname) {
            let zIndex = -1;
            if (iframeElem && iframeElem.style && iframeElem.style.zIndex) {
                zIndex = parseInt(iframeElem.style.zIndex);
            }
            var toReturn = {
                Index: -1,
                IframeElem: iframeElem,
                Id: this.HelperAgent.GuidHelper.NewGuid(),
                Zindex: zIndex,
                Nickname: nickname,
                ContentDoc: null,
            };
            toReturn.ContentDoc = this.DataOneContentDocFactoryFromIframe(toReturn);
            this.Logger.FuncEnd(this.DataOneIframeFactory.name);
        }
        else {
            this.Logger.ErrorAndThrow(this.DataOneIframeFactory.name, 'one of these is null');
            this.Logger.LogAsJsonPretty('iframeElem', iframeElem);
            this.Logger.LogAsJsonPretty('nickname', nickname);
        }
        return toReturn;
    }
    CreateShellIDataOneWindowStorage(windowType, flavor) {
        this.Logger.FuncStart(this.CreateShellIDataOneWindowStorage.name);
        var dateToUse = new Date();
        var newGuid = this.HelperAgent.GuidHelper.NewGuid();
        var activeWindowSnapShot = {
            TimeStamp: dateToUse,
            WindowType: windowType,
            WindowFriendly: windowType[windowType],
            AllCEAr: [],
            Id: newGuid,
            NickName: '',
            RawData: null,
            Flavor: flavor,
        };
        this.Logger.FuncEnd(this.CreateShellIDataOneWindowStorage.name);
        return activeWindowSnapShot;
    }
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Shared/scripts/Helpers/UrlHelper.js



class UrlHelper_UrlHelper extends HelperBase {
    MakeUrlParts(url) {
        var parser = document.createElement('a');
        parser.href = url.AbsUrl;
        let toReturn = {
            OriginalRaw: url.AbsUrl,
            Protocol: parser.protocol,
            HostAndPort: parser.host,
            Parameters: this.ExtractParameters(parser.search),
            FilePath: parser.pathname,
            Anchor: parser.hash,
            ScWindowType: scWindowType.Unknown,
            HasError: false,
        };
        toReturn.ScWindowType = this.CalcPageTypeFromHref(url);
        return toReturn;
    }
    CloneUrlParts(original) {
        let toReturn = {
            Anchor: original.Anchor,
            FilePath: original.FilePath,
            HasError: original.HasError,
            HostAndPort: original.HostAndPort,
            OriginalRaw: original.OriginalRaw,
            Parameters: original.Parameters,
            Protocol: original.Protocol,
            ScWindowType: original.ScWindowType,
        };
        return toReturn;
    }
    BuildEditPrevNormUrl(scMode, contState, urlParts) {
        let toReturn = urlParts;
        urlParts.Anchor = '';
        urlParts.FilePath = '';
        urlParts.Parameters = [];
        urlParts.ScWindowType = scWindowType.Unknown;
        urlParts.Parameters.push({
            Key: 'sc_itemid',
            value: contState.ActiveCe.ActiveNode.NodeId.AsBracedGuid
        });
        urlParts.Parameters.push({
            Key: 'sc_mode',
            value: scMode.AsString
        });
        urlParts.Parameters.push({
            Key: 'sc_lang',
            value: 'en'
        });
        urlParts.Parameters.push({
            Key: 'sc_site',
            value: 'website'
        });
        return toReturn;
    }
    BuildFullUrlFromParts(UrlParts) {
        let toReturn = {
            AbsUrl: '',
        };
        if (UrlParts && !UrlParts.HasError) {
            toReturn.AbsUrl = UrlParts.Protocol + '//' + UrlParts.HostAndPort;
            if (UrlParts.FilePath.length > 0) {
                toReturn.AbsUrl += UrlParts.FilePath;
            }
            if (UrlParts.Parameters && UrlParts.Parameters.length > 0) {
                toReturn.AbsUrl += '?';
                for (var idx = 0; idx < UrlParts.Parameters.length; idx++) {
                    if (idx > 0) {
                        toReturn.AbsUrl += '&';
                    }
                    let oneParam = UrlParts.Parameters[idx];
                    toReturn.AbsUrl += oneParam.Key;
                    if (oneParam.value) {
                        toReturn.AbsUrl += '=' + oneParam.value;
                    }
                }
            }
            if (UrlParts.Anchor.length > 0) {
                toReturn.AbsUrl += '#' + UrlParts.Anchor;
            }
        }
        return toReturn;
    }
    SetScModeFromEditPrevNorm(newValue, urlParts) {
        let toReturn = urlParts;
        if (urlParts && newValue) {
            urlParts = this.SetFilePathFromWindowType(urlParts);
            if (urlParts && urlParts.Parameters && urlParts.Parameters.length > 0)
                for (var idx = 0; idx < urlParts.Parameters.length; idx++) {
                    let candidate = urlParts.Parameters[idx];
                    if (candidate.Key === 'scmode') {
                        candidate.value = newValue.AsString;
                        break;
                    }
                }
        }
        return toReturn;
    }
    SetFilePathFromWindowType(urlParts, windowType = null) {
        let toReturn = urlParts;
        if (!windowType) {
            windowType = urlParts.ScWindowType;
        }
        toReturn.ScWindowType = windowType;
        switch (toReturn.ScWindowType) {
            case scWindowType.ContentEditor:
                toReturn.FilePath = SharedConst.Const.UrlSuffix.CE;
                break;
            case scWindowType.Desktop:
                toReturn.FilePath = SharedConst.Const.UrlSuffix.Desktop;
                break;
            case scWindowType.Edit:
                toReturn.FilePath = SharedConst.Const.UrlSuffix.None;
                break;
            case scWindowType.Preview:
                toReturn.FilePath = SharedConst.Const.UrlSuffix.None;
                break;
            case scWindowType.Normal:
                toReturn.FilePath = SharedConst.Const.UrlSuffix.None;
                break;
            default:
                toReturn.FilePath = '';
                this.Logger.ErrorAndThrow(this.SetFilePathFromWindowType.name, 'unaccounted for window type');
                break;
        }
        return toReturn;
    }
    ExtractParameters(url) {
        let toReturn = [];
        if (url) {
            let splitStr = url.split('?');
            if (splitStr.length > 1) {
                let paramString = splitStr[1].replace(SharedConst.Const.Regex.QueryStrSeparatorQuest, '');
                let pairStr = paramString.split('&');
                if (pairStr && pairStr.length > 0) {
                    for (var idx = 0; idx < pairStr.length; idx++) {
                        let oneParamAr = pairStr[idx].split('=');
                        let paramPair = {
                            Key: '',
                            value: ''
                        };
                        if (oneParamAr) {
                            paramPair.Key = oneParamAr[0];
                            if (oneParamAr.length > 1) {
                                paramPair.value = oneParamAr[1];
                            }
                            toReturn.push(paramPair);
                        }
                    }
                }
            }
        }
        return toReturn;
    }
    ExtractFilePath(url, parser) {
        let toReturn = '';
        parser.protocol;
        parser.hostname;
        parser.port;
        parser.pathname;
        parser.search;
        parser.hash;
        parser.host;
        toReturn = parser.pathname;
        return toReturn;
    }
    ExtractProtocol(url) {
        let toReturn = '';
        if (url.startsWith('https')) {
            toReturn = 'https';
        }
        else {
            toReturn = 'http';
        }
        return toReturn;
    }
    ExtractHostName(url) {
        let toReturn = '';
        if (url.indexOf('//') > -1) {
            toReturn = url.split('/')[2];
        }
        else {
            toReturn = url.split('/')[0];
        }
        toReturn = toReturn.split(':')[0];
        toReturn = toReturn.split('?')[0];
        return toReturn;
    }
    __urlVsRegex(regexPattern, url) {
        return new RegExp(regexPattern).test(url);
    }
    CalcPageTypeFromHref(absUrl) {
        this.Logger.FuncStart(this.CalcPageTypeFromHref.name);
        var toReturn = scWindowType.Unknown;
        if (absUrl && absUrl.AbsUrl) {
            this.Logger.LogVal('current url', absUrl.AbsUrl);
            if (absUrl.AbsUrl.indexOf(SharedConst.Const.UrlSuffix.Login) > -1) {
                toReturn = scWindowType.LoginPage;
            }
            else if (new RegExp(SharedConst.Const.Regex.ContentEditor).test(absUrl.AbsUrl)) {
                toReturn = scWindowType.ContentEditor;
            }
            else if (absUrl.AbsUrl.toLowerCase().indexOf(SharedConst.Const.UrlSuffix.LaunchPad.toLowerCase()) > -1) {
                toReturn = scWindowType.Launchpad;
            }
            else if (this.__urlVsRegex(SharedConst.Const.Regex.PageType.Desktop, absUrl.AbsUrl)) {
                toReturn = scWindowType.Desktop;
            }
            else if (this.__urlVsRegex(SharedConst.Const.Regex.PageType.Preview, absUrl.AbsUrl)) {
                toReturn = scWindowType.Preview;
            }
            else if (this.__urlVsRegex(SharedConst.Const.Regex.PageType.Edit, absUrl.AbsUrl)) {
                toReturn = scWindowType.Edit;
            }
            else if (this.__urlVsRegex(SharedConst.Const.Regex.PageType.Normal, absUrl.AbsUrl)) {
                toReturn = scWindowType.Normal;
            }
            else {
                toReturn = scWindowType.Unknown;
            }
        }
        else {
            this.Logger.ErrorAndThrow(this.CalcPageTypeFromHref.name, 'null url');
        }
        this.Logger.FuncEnd(this.CalcPageTypeFromHref.name, scWindowType[toReturn]);
        return toReturn;
    }
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Shared/scripts/Classes/PromisesRecipes.js
var PromisesRecipes_awaiter = (undefined && undefined.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};



class PromisesRecipes_PromisesRecipes extends HelperBase {
    FromDesktopOpenNewCEIframe(targetDoc) {
        return PromisesRecipes_awaiter(this, void 0, void 0, function* () {
            return new Promise((resolve, reject) => {
                let allIframeDataAtBeginning;
                let targetCeAgent;
                let newIframe;
                this.HelperAgent.PromisesBasic.GetAllLiveIframeData(targetDoc)
                    .then((result) => allIframeDataAtBeginning = result)
                    .then(() => this.HelperAgent.PromisesBasic.RaceWaitAndClick(InjectConst_ContentConst.Const.Selector.SC.scStartButton, targetDoc))
                    .then(() => this.HelperAgent.PromisesBasic.WaitForThenClick([InjectConst_ContentConst.Const.Selector.SC.StartMenuLeftOption], targetDoc))
                    .then(() => this.HelperAgent.PromisesBasic.WaitForNewIframe(allIframeDataAtBeginning, targetDoc))
                    .then((result) => newIframe = result)
                    .then(() => this.HelperAgent.PromisesBasic.WaitForReadyIframe(newIframe))
                    .then(() => targetCeAgent = new OneCEAgent_OneCEAgent(newIframe.ContentDoc, this.Logger, this.HelperAgent))
                    .then(() => resolve(targetCeAgent))
                    .catch((err) => { throw this.FromDesktopOpenNewCEIframe.name + ' ' + err; });
            });
        });
    }
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Shared/scripts/Helpers/Helpers.js






class Helpers_HelperAgent {
    constructor(logger) {
        logger.FuncStart(Helpers_HelperAgent.name);
        this.Init(logger, this);
        logger.FuncEnd(Helpers_HelperAgent.name);
    }
    Init(logger, helperAgent) {
        this.GuidHelper = new GuidHelper_GuidHelper(logger, helperAgent);
        this.UtilityHelp = new UtilityHelper_UtilityHelper(logger, helperAgent);
        this.PromisesBasic = new PromiseGeneric_PromisesBasic(logger, helperAgent);
        this.PromisesRecipes = new PromisesRecipes_PromisesRecipes(logger, helperAgent);
        this.FactoryHelp = new FactoryHelper_FactoryHelper(logger, helperAgent);
        this.UrlHelp = new UrlHelper_UrlHelper(logger, helperAgent);
    }
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Shared/scripts/Agents/Agents/AllAgents.js
class AllAgents_AllAgents {
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Shared/scripts/Agents/Drones/RollingLogIdDrone.js

class RollingLogIdDrone_RollingLogIdDrone {
    constructor(settingsAgent) {
        this.SettingsAgent = settingsAgent;
    }
    GetNextLogId() {
        let nextKeyInt = 0;
        let nextKeyToReturn;
        var result = this.GetLastUsedLogId().ValueAsInt();
        nextKeyInt = result + 1;
        if (nextKeyInt > 10) {
            nextKeyInt = 0;
        }
        this.SettingsAgent.SetByKey(SettingKey_SettingKey.LastUsedLogToStorageKey, nextKeyInt.toString());
        nextKeyToReturn = nextKeyInt.toString();
        return nextKeyToReturn;
    }
    GetLastUsedLogId() {
        let toReturn = 0;
        console.log("getting last");
        console.log('Using Key : ' + SettingKey_SettingKey.LastUsedLogToStorageKey);
        var lastUsedLogIdSetting = this.SettingsAgent.GetByKey(SettingKey_SettingKey.LastUsedLogToStorageKey);
        console.log('storage results (value): ' + lastUsedLogIdSetting.ValueAsInt());
        console.log(JSON.stringify(lastUsedLogIdSetting));
        return lastUsedLogIdSetting;
    }
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Shared/scripts/Agents/Agents/SettingsAgent/OneGenericSetting.js
class OneGenericSetting {
    constructor(settingKey, dataType, valueAsObj, uiSelector, defaultValue, settingFlavor, friendly, hasUi = true) {
        this.SettingKey = settingKey;
        this.DataType = dataType;
        this.ValueAsObj = valueAsObj;
        this.UiSelector = uiSelector;
        this.DefaultValue = defaultValue;
        this.SettingFlavor = settingFlavor;
        this.Friendly = friendly;
        this.HasUi = hasUi;
    }
    ValueAsInt() {
        var toReturn = parseInt(this.ValueAsObj.toString());
        return toReturn;
    }
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Shared/scripts/Enums/SettingType.js
var SettingType;
(function (SettingType) {
    SettingType[SettingType["Unknown"] = 0] = "Unknown";
    SettingType[SettingType["BoolCheckBox"] = 1] = "BoolCheckBox";
    SettingType[SettingType["Accordion"] = 2] = "Accordion";
    SettingType[SettingType["Number"] = 3] = "Number";
})(SettingType || (SettingType = {}));

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Shared/scripts/Agents/Agents/SettingsAgent/ConstAllSettings.js






class ConstAllSettings_ConstAllSettings {
    constructor() {
        this.AllSettings = [
            new OneGenericSetting(SettingKey_SettingKey.LastUsedLogToStorageKey, SettingType.Number, 0, null, 0, SettingFlavor.ContentAndPopUpStoredInEach, 'Rolling Prefix key for log to storage', false),
            new OneGenericSetting(SettingKey_SettingKey.Test, SettingType.BoolCheckBox, null, PopConst_PopConst.Const.Selector.HS.GenericSettingTest, false, SettingFlavor.PopUp, 'Generic Setting Test'),
            new OneGenericSetting(SettingKey_SettingKey.LogToConsole, SettingType.BoolCheckBox, true, PopConst_PopConst.Const.Selector.HS.LogToConsole, SharedConst.Const.Settings.Defaults.LogToConsole, SettingFlavor.ContentAndPopUpStoredInPopUp, 'Log to Console'),
            new OneGenericSetting(SettingKey_SettingKey.AutoSnapshotBeforeWindowChange, SettingType.BoolCheckBox, null, PopConst_PopConst.Const.Selector.HS.AutoSnapshotBeforeWindowChange, PopConst_PopConst.Const.Storage.Defaults.bool.AutoSnapshotBeforeWindowChange, SettingFlavor.ContentAndPopUpStoredInPopUp, 'Auto Save Snapshot on HindSite Window Change'),
            new OneGenericSetting(SettingKey_SettingKey.AutoLogin, SettingType.BoolCheckBox, null, PopConst_PopConst.Const.Selector.HS.iCBoxdSettingsAutoLogin, PopConst_PopConst.Const.Storage.Defaults.bool.AutoLogin, SettingFlavor.ContentAndPopUpStoredInPopUp, 'Auto Login'),
            new OneGenericSetting(SettingKey_SettingKey.LgndLog, SettingType.Accordion, null, PopConst_PopConst.Const.Selector.Legend.LgndDebug, PopConst_PopConst.Const.Settings.Defaults.LgndDebug, SettingFlavor.PopUp, 'Log'),
            new OneGenericSetting(SettingKey_SettingKey.LgndForeSite, SettingType.Accordion, null, PopConst_PopConst.Const.Selector.Legend.LgndForeSite, PopConst_PopConst.Const.Settings.Defaults.LgndForeSite, SettingFlavor.PopUp, 'Fore&bull;Site'),
            new OneGenericSetting(SettingKey_SettingKey.LgndHindSite, SettingType.Accordion, null, PopConst_PopConst.Const.Selector.Legend.LgndHindSite, PopConst_PopConst.Const.Settings.Defaults.LgndHindSite, SettingFlavor.PopUp, 'Hind&bull;Site'),
            new OneGenericSetting(SettingKey_SettingKey.LgndHindSite, SettingType.Accordion, null, PopConst_PopConst.Const.Selector.Legend.LgndInSite, PopConst_PopConst.Const.Settings.Defaults.LgndInSite, SettingFlavor.PopUp, 'In&bull;Site'),
            new OneGenericSetting(SettingKey_SettingKey.LgndSettings, SettingType.Accordion, null, PopConst_PopConst.Const.Selector.Legend.LgndSettings, PopConst_PopConst.Const.Settings.Defaults.LgndSettings, SettingFlavor.PopUp, 'Settings'),
            new OneGenericSetting(SettingKey_SettingKey.LgndState, SettingType.Accordion, null, PopConst_PopConst.Const.Selector.Legend.LgndState, PopConst_PopConst.Const.Settings.Defaults.LgndState, SettingFlavor.PopUp, 'State'),
            new OneGenericSetting(SettingKey_SettingKey.MaxAutoSaveCount, SettingType.Number, null, PopConst_PopConst.Const.Selector.HS.SettingAutoSaveMaxCount, PopConst_PopConst.Const.Numbers.MaxAutoSaveCount, SettingFlavor.ContentAndPopUpStoredInPopUp, 'Auto Save Max Count'),
            new OneGenericSetting(SettingKey_SettingKey.AutoSaveIntervalMin, SettingType.Number, null, PopConst_PopConst.Const.Selector.HS.SettingAutoSaveInterval, PopConst_PopConst.Const.Numbers.AutoSaveIntervalMin, SettingFlavor.ContentAndPopUpStoredInPopUp, 'Auto Save Interval (Min) - 0 to disable'),
            new OneGenericSetting(SettingKey_SettingKey.DebugKeepDialogOpen, SettingType.BoolCheckBox, null, PopConst_PopConst.Const.Selector.HS.SettingDebugKeepDialogOpen, PopConst_PopConst.Const.Settings.Defaults.DebugKeepDialogOpen, SettingFlavor.PopUp, '(Debug) Keep Dialog Open')
        ];
    }
}

// CONCATENATED MODULE: ./AutoBuild/TsTranspiled/Content/scripts/zContentEntryPoint.js
var zContentEntryPoint_awaiter = (undefined && undefined.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};








function main() {
    return zContentEntryPoint_awaiter(this, void 0, void 0, function* () {
        var allAgents = new AllAgents_AllAgents();
        allAgents.Logger = new LoggerAgent_LoggerAgent();
        allAgents.RepoAgent = new RepoAgent_RepoAgent(allAgents.Logger);
        allAgents.SettingsAgent = new SettingsAgent_SettingsAgent(allAgents.Logger, allAgents.RepoAgent);
        var allSettings = new ConstAllSettings_ConstAllSettings().AllSettings;
        yield allAgents.SettingsAgent.InitSettingsAgent(allSettings);
        var RollingLogId = new RollingLogIdDrone_RollingLogIdDrone(allAgents.SettingsAgent);
        var nextLogId = RollingLogId.GetNextLogId();
        console.log('RollingLogId: ' + nextLogId);
        allAgents.Logger.SetLogToStorageKey(nextLogId);
        allAgents.HelperAgent = new Helpers_HelperAgent(allAgents.Logger);
        allAgents.Logger.ThrowIfNullOrUndefined("allAgents", allAgents);
        allAgents.Logger.ThrowIfNullOrUndefined("allAgents.HelperAgent", allAgents.HelperAgent);
        new ContentHub_ContentHub(allAgents);
    });
}
main();


/***/ })
/******/ ]);